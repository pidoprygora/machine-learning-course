# Fine-Tuning Попередньо Навчених CNN

## Опис проєкту

Цей проєкт демонструє комплексний бенчмаркінг різних стратегій **Fine-Tuning** попередньо навчених CNN на специфічних категоріях зображень. Фокус на **оптимізації параметрів**, **аналізі помилок класифікації** та порівнянні різних архітектур.

## Особливості проєкту

✨ **Основні можливості:**
- 🎯 4 спеціалізовані датасети (транспорт, тварини, взуття, люди)
- 🧠 4 архітектури CNN (VGG16, ResNet50, MobileNetV2, EfficientNetB0)
- 🔧 3 стратегії fine-tuning (frozen, partial, full)
- 📊 Візуалізація процесу оптимізації параметрів
- 🔍 Confusion matrices для всіх моделей
- ❌ Аналіз найтиповіших помилок класифікації
- 📈 Детальне порівняння метрик (Accuracy, Precision, Recall, F1)
- ⚡ Оцінка швидкості навчання

## Концепція Fine-Tuning

### Що таке Fine-Tuning?

**Fine-Tuning** — це техніка transfer learning, де ми берем попередньо навчену модель (наприклад, на ImageNet з 1000 класів) та адаптуємо її до нашої специфічної задачі з меншою кількістю даних.

### Чому це працює?

1. **Нижні шари** навчають універсальні візуальні ознаки (краї, кути, текстури)
2. **Середні шари** навчають більш складні патерни (форми, частини об'єктів)
3. **Верхні шари** навчають високорівневі features специфічні для датасету

При fine-tuning ми зберігаємо універсальні знання з нижніх шарів та адаптуємо верхні шари під нашу задачу.

### Переваги Fine-Tuning:

✅ **Менше даних** - не потрібні мільйони зображень  
✅ **Швидше навчання** - стартуємо з хороших ваг  
✅ **Краща точність** - використовуємо знання з ImageNet  
✅ **Менше overfitting** - regularization від pretrained ваг  

## Стратегії Fine-Tuning

### 1. Frozen Base (Заморожені базові шари) ❄️

**Опис:** Заморожуємо всі шари базової моделі, навчаємо тільки новий classifier head.

**Параметри:**
- `base_model.trainable = False`
- Learning rate: `1e-3` (високий)
- Швидке навчання (5-10 epochs)

**Коли використовувати:**
- ✅ Датасет дуже схожий на ImageNet
- ✅ Мало даних (< 5000 зображень)
- ✅ Обмежені обчислювальні ресурси
- ✅ Потрібна швидкість

**Переваги:**
- Дуже швидке навчання
- Не потребує багато пам'яті
- Не overfit на малих датасетах

**Недоліки:**
- Не адаптується до специфіки датасету
- Нижча точність на складних задачах

### 2. Partial Fine-Tuning (Часткове розморожування) 🔓

**Опис:** Заморожуємо перші 70-80% шарів, розморожуємо останні 20-30%.

**Параметри:**
- Заморожено: перші 80% шарів
- Trainable: останні 20% шарів + classifier
- Learning rate: `1e-4` (середній)
- Середнє навчання (10-20 epochs)

**Коли використовувати:**
- ✅ Датасет трохи відрізняється від ImageNet
- ✅ Середня кількість даних (5k-20k)
- ✅ Є GPU для навчання
- ✅ Потрібен баланс якості/швидкості

**Переваги:**
- Адаптується до специфіки датасету
- Кращий баланс швидкості та якості
- Рекомендований підхід для більшості задач

**Недоліки:**
- Повільніше за frozen
- Може overfit при малій кількості даних

### 3. Full Fine-Tuning (Повне розморожування) 🔥

**Опис:** Розморожуємо всі шари та навчаємо з дуже низьким learning rate.

**Параметри:**
- `base_model.trainable = True`
- Learning rate: `1e-5` (дуже низький)
- Довге навчання (20-50 epochs)

**Коли використовувати:**
- ✅ Датасет сильно відрізняється від ImageNet
- ✅ Багато даних (> 20k зображень)
- ✅ Потужні обчислювальні ресурси
- ✅ Потрібна максимальна точність

**Переваги:**
- Максимальна адаптація до датасету
- Найвища можлива точність
- Навчає domain-specific features

**Недоліки:**
- Дуже повільне навчання
- Потребує багато пам'яті
- Ризик overfitting

## Датасети

Проєкт використовує 4 спеціалізовані датасети створені з CIFAR-10, CIFAR-100 та Fashion-MNIST:

### 1. Транспорт (CIFAR-10) 🚗

**Опис:** Різні види транспорту

**Класи (4):**
- Літак ✈️
- Автомобіль 🚗
- Корабель 🚢
- Вантажівка 🚚

**Розмір:**
- Train: 8,000 зображень
- Test: 2,000 зображень
- Shape: 32×32×3 (RGB)

**Складність:** Середня  
**Виклики:** Відрізнити автомобіль від вантажівки, літак від корабля (ракурси)

### 2. Тварини (CIFAR-10) 🐾

**Опис:** Різні види тварин

**Класи (6):**
- Птах 🐦
- Кіт 🐱
- Олень 🦌
- Собака 🐕
- Жаба 🐸
- Кінь 🐴

**Розмір:**
- Train: 10,000 зображень
- Test: 2,500 зображень
- Shape: 32×32×3 (RGB)

**Складність:** Висока  
**Виклики:** Кіт vs собака, олень vs кінь — схожі форми та текстури

### 3. Взуття (Fashion-MNIST) 👟

**Опис:** Різні види взуття

**Класи (3):**
- Сандалі 🩴
- Кросівки 👟
- Черевики 👢

**Розмір:**
- Train: 10,000 зображень
- Test: 2,500 зображень
- Shape: 28×28×3 (Grayscale → RGB)

**Складність:** Низька  
**Виклики:** Чорно-білі зображення, перетворені в RGB; розрізнити кросівки та черевики

### 4. Люди (CIFAR-100) 👨👩

**Опис:** Обличчя людей різного віку та статі

**Класи (5):**
- Немовля 👶
- Хлопчик 👦
- Дівчинка 👧
- Чоловік 👨
- Жінка 👩

**Розмір:**
- Train: ~2,500 зображень
- Test: ~500 зображень
- Shape: 32×32×3 (RGB)

**Складність:** Дуже висока  
**Виклики:** Дуже мало даних, складно розрізнити вік та стать на низькій роздільній здатності

## Моделі

### 1. VGG16 🏛️

**Параметри:**
- **Рік**: 2014 (Oxford VGG Group)
- **Шари**: 16 (13 conv + 3 FC)
- **Параметри**: ~14.7M (base) + ~1M (head)
- **Features**: 512-D після GAP
- **ImageNet Top-5**: 90.1%

**Архітектура:**
```
Input (32×32×3)
  ↓
Block 1: Conv(64)×2 → MaxPool
Block 2: Conv(128)×2 → MaxPool
Block 3: Conv(256)×3 → MaxPool
Block 4: Conv(512)×3 → MaxPool
Block 5: Conv(512)×3 → MaxPool
  ↓
GlobalAveragePooling
  ↓
Dense(256) → Dropout(0.5)
Dense(128) → Dropout(0.3)
Dense(n_classes) → Softmax
```

**Особливості:**
- ✅ Проста та зрозуміла архітектура
- ✅ Глибокі features, багато шарів
- ✅ Дуже стабільна та надійна
- ⚠️ Велика кількість параметрів
- ⚠️ Повільна інференція

**Найкраще для:**
- Задач де важлива якість над швидкістю
- Датасети з багатьма деталями
- Коли є потужний GPU

### 2. ResNet50 🚀

**Параметри:**
- **Рік**: 2015 (Microsoft Research)
- **Шари**: 50 (з residual connections)
- **Параметри**: ~25.6M (base) + ~1M (head)
- **Features**: 2048-D після GAP
- **ImageNet Top-5**: 93.3%

**Архітектура:**
```
Input (32×32×3)
  ↓
Conv(64) → BatchNorm → ReLU
  ↓
Residual Blocks:
  Stage 1: [64, 64, 256] × 3
  Stage 2: [128, 128, 512] × 4
  Stage 3: [256, 256, 1024] × 6
  Stage 4: [512, 512, 2048] × 3
  ↓
GlobalAveragePooling
  ↓
Dense(256) → Dropout(0.5)
Dense(128) → Dropout(0.3)
Dense(n_classes) → Softmax
```

**Особливості:**
- ✅ Skip connections - вирішують gradient vanishing
- ✅ Дуже глибока (50 шарів)
- ✅ Найвищої якості features
- ✅ Ефективніша за VGG16
- ⚠️ Багато параметрів

**Найкраще для:**
- Складні візуальні задачі
- Коли потрібна максимальна точність
- Transfer learning (найпопулярніший вибір)

### 3. MobileNetV2 ⚡

**Параметри:**
- **Рік**: 2018 (Google)
- **Шари**: 53 (з inverted residuals)
- **Параметри**: ~3.5M (base) + ~1M (head)
- **Features**: 1280-D після GAP
- **ImageNet Top-5**: 90.0%

**Архітектура:**
```
Input (32×32×3)
  ↓
Conv(32)
  ↓
Inverted Residual Blocks (17×):
  1×1 Conv (expand) → 3×3 DepthwiseConv → 1×1 Conv (project)
  + Skip connection
  ↓
Conv(1280)
  ↓
GlobalAveragePooling
  ↓
Dense(256) → Dropout(0.5)
Dense(128) → Dropout(0.3)
Dense(n_classes) → Softmax
```

**Особливості:**
- ✅ Depthwise Separable Convolutions
- ✅ Дуже мало параметрів (3.5M)
- ✅ Швидка інференція
- ✅ Ефективна для мобільних пристроїв
- ⚠️ Трохи нижча точність за ResNet

**Найкраще для:**
- Обмежені ресурси (CPU, мобільні)
- Real-time застосунки
- Коли важлива швидкість
- Deployment на edge devices

### 4. EfficientNetB0 🎯

**Параметри:**
- **Рік**: 2019 (Google Brain)
- **Шари**: Adaptive (compound scaling)
- **Параметри**: ~5.3M (base) + ~1M (head)
- **Features**: 1280-D після GAP
- **ImageNet Top-5**: 93.3%

**Архітектура:**
```
Input (32×32×3)
  ↓
Conv(32)
  ↓
MBConv Blocks (16×):
  Squeeze-and-Excitation
  Depthwise Separable Conv
  Skip connections
  ↓
Conv(1280)
  ↓
GlobalAveragePooling
  ↓
Dense(256) → Dropout(0.5)
Dense(128) → Dropout(0.3)
Dense(n_classes) → Softmax
```

**Особливості:**
- ✅ Compound scaling (depth + width + resolution)
- ✅ Squeeze-and-Excitation blocks
- ✅ Найкращий баланс точності/ефективності
- ✅ State-of-the-art при малих розмірах
- ✅ Невелика кількість параметрів

**Найкраще для:**
- Коли потрібен найкращий баланс
- Сучасні production системи
- Коли важлива і якість, і швидкість

## Візуалізації

### 1. Історія Навчання 📈

**Файл**: `training_history_*.png`

**4 графіки:**
- **Train Accuracy** - точність на навчальній вибірці
- **Validation Accuracy** - точність на валідаційній вибірці
- **Train Loss** - loss на навчальній вибірці
- **Validation Loss** - loss на валідаційній вибірці

**Що шукати:**
- ✅ **Зростання accuracy** — модель навчається
- ✅ **Зниження loss** — оптимізація працює
- ⚠️ **Розрив train-val** — ознака overfitting
- ⚠️ **Плато** — модель досягла межі

### 2. Оптимізація Параметрів 🔧

**Файл**: `weight_optimization_*.png`

**Опис:** Візуалізує зміну L2 норми ваг для різних шарів під час навчання.

**Що показує:**
- Як параметри змінюються з кожною епохою
- Які шари навчаються найактивніше
- Стабільність процесу оптимізації

**Інтерпретація:**
- ✅ **Плавні зміни** — стабільне навчання
- ✅ **Конвергенція** — модель досягає оптимуму
- ⚠️ **Різкі стрибки** — нестабільність (LR завеликий)
- ⚠️ **Відсутність змін** — шар заморожений або gradient vanishing

### 3. Confusion Matrices 🔢

**Файл**: `confusion_matrices_*.png`

**Опис:** Матриці плутанини для всіх моделей (нормалізовані по рядках).

**Як читати:**
- **Діагональ** — правильні передбачення (має бути темно-синя)
- **Поза діагоналлю** — помилки
- **Рядки** — справжні класи
- **Колонки** — передбачені класи

**Приклад аналізу:**
```
         Літак  Авто  Корабель  Вантажівка
Літак      0.95  0.00    0.03       0.02    ← 95% правильно
Авто       0.00  0.88    0.00       0.12    ← 12% сплутано з вантажівкою
Корабель   0.05  0.00    0.92       0.03
Вантажівка 0.00  0.15    0.00       0.85    ← 15% сплутано з авто
```

**Типові помилки:**
- Автомобіль ↔ Вантажівка (схожі)
- Кіт ↔ Собака (складно розрізнити)
- Хлопчик ↔ Чоловік (вік важко визначити)

### 4. Аналіз Помилок ❌

**Файл**: `errors_*_*.png`

**Опис:** Топ-20 найвпевненіших помилок класифікації.

**Що показує:**
- Зображення, де модель помилилася
- Справжній клас vs передбачений
- Впевненість моделі (confidence)

**Приклад:**
```
True: Собака
Pred: Кіт
Conf: 0.87
```
Модель з 87% впевненістю неправильно класифікувала собаку як кота.

**Чому це важливо:**
- Розуміємо слабкі місця моделі
- Знаходимо проблемні класи
- Можемо покращити датасет (додати складніші приклади)

### 5. Порівняння Стратегій 🔄

**Файл**: `strategy_comparison_*.png`

**3 графіки:**
1. **Validation Accuracy** — як різні стратегії навчаються
2. **Фінальні метрики** — Accuracy, Precision, Recall, F1
3. **Кількість епох** — скільки потрібно до early stopping

**Висновки:**
- `frozen` — швидко, але нижча точність
- `partial` — найкращий баланс (рекомендовано)
- `full` — найвища точність, але довго

### 6. Фінальне Порівняння 🏆

**Файл**: `final_comparison.png`

**Опис:** Порівняння всіх моделей на всіх датасетах.

**2 графіки:**
- **Accuracy по датасетах** — бар-чарт для кожної моделі
- **F1-Score по датасетах** — зважена метрика

**Виводи:**
- Яка модель найкраща для кожного типу задач
- Який датасет найскладніший
- Trade-offs між моделями

## Метрики

### 1. Accuracy (Точність)

**Формула:** `(TP + TN) / (TP + TN + FP + FN)`

**Опис:** Частка правильних передбачень серед усіх.

**Коли використовувати:**
- ✅ Збалансовані класи
- ✅ Проста інтерпретація

**Коли НЕ використовувати:**
- ❌ Незбалансовані класи (наприклад, 95% один клас)

### 2. Precision (Точність)

**Формула:** `TP / (TP + FP)`

**Опис:** Серед усіх передбачень класу, скільки правильних?

**Коли важливо:**
- Коли важливо уникати false positives
- Медична діагностика
- Spam detection

### 3. Recall (Повнота)

**Формула:** `TP / (TP + FN)`

**Опис:** Серед усіх справжніх представників класу, скільки знайдено?

**Коли важливо:**
- Коли важливо знайти всі позитивні випадки
- Виявлення хвороб
- Пошук об'єктів

### 4. F1-Score

**Формула:** `2 × (Precision × Recall) / (Precision + Recall)`

**Опис:** Гармонійне середнє precision та recall.

**Переваги:**
- ✅ Балансує precision та recall
- ✅ Краще для незбалансованих класів
- ✅ Одне число замість двох

**Коли використовувати:**
- Коли важливі і precision, і recall
- Незбалансовані датасети
- Загальна оцінка якості

## Структура проєкту

```
Deep-learning/03-Fine-tuning/
│
├── main.py                                      # Головний скрипт
├── requirements.txt                             # Залежності
├── README.md                                    # Документація
│
└── results/                                     # Згенеровані файли
    ├── dataset_samples.png                      # Приклади датасетів
    │
    ├── training_history_Транспорт_CIFAR-10.png # Історія: Транспорт
    ├── training_history_Тварини_CIFAR-10.png   # Історія: Тварини
    ├── training_history_Взуття_Fashion-MNIST.png # Історія: Взуття
    ├── training_history_Люди_CIFAR-100.png     # Історія: Люди
    │
    ├── weight_optimization_*.png                # Оптимізація параметрів
    │
    ├── confusion_matrices_*.png                 # Матриці плутанини
    │
    ├── errors_*_VGG16.png                      # Помилки VGG16
    ├── errors_*_ResNet50.png                   # Помилки ResNet50
    ├── errors_*_MobileNetV2.png                # Помилки MobileNetV2
    ├── errors_*_EfficientNetB0.png             # Помилки EfficientNetB0
    │
    ├── strategy_comparison_*.png                # Порівняння стратегій
    │
    ├── final_comparison.png                     # Фінальне порівняння
    │
    ├── benchmark_*.csv                          # Метрики по датасетах
    ├── final_accuracy_comparison.csv            # Зведена таблиця Accuracy
    └── final_f1_comparison.csv                  # Зведена таблиця F1
```

## Встановлення

### Вимоги:
- Python 3.8+
- TensorFlow 2.x
- GPU (опціонально, але рекомендується)

### Кроки:

```bash
# Перейти до директорії
cd Deep-learning/03-Fine-tuning

# (Опціонально) Віртуальне середовище
python -m venv venv

# Активація
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Встановлення залежностей
pip install -r requirements.txt
```

## Запуск

```bash
python main.py
```

**Що відбувається:**
1. 📥 Завантаження датасетів (CIFAR-10, CIFAR-100, Fashion-MNIST)
2. 🎨 Візуалізація прикладів
3. 🔄 Порівняння стратегій fine-tuning (frozen, partial, full)
4. 🧠 Навчання 4 моделей на 4 датасетах (16 експериментів)
5. 📊 Обчислення метрик (Accuracy, Precision, Recall, F1)
6. 🔍 Візуалізація confusion matrices
7. ❌ Аналіз найтиповіших помилок
8. 📈 Створення порівняльних графіків
9. 💾 Збереження результатів

**Очікуваний час виконання:**
- GPU (RTX 3060+): ~30-40 хвилин
- CPU: ~2-3 години

## Очікувані результати

### Транспорт (CIFAR-10) 🚗

**Складність:** Середня

**Найкраща модель:** ResNet50 / EfficientNetB0  
**Очікуваний Accuracy:** 85-92%

**Типові помилки:**
- Автомобіль ↔ Вантажівка (схожі форми)
- Літак ↔ Корабель (нестандартні ракурси)

**Інсайти:**
- Pretrained features добре узагальнюються
- Partial fine-tuning достатньо
- MobileNetV2 також показує гарні результати

### Тварини (CIFAR-10) 🐾

**Складність:** Висока

**Найкраща модель:** ResNet50  
**Очікуваний Accuracy:** 75-85%

**Типові помилки:**
- Кіт ↔ Собака (дуже схожі на 32×32)
- Олень ↔ Кінь (чотирилапі тварини)
- Птах ↔ Жаба (різні, але мала роздільна здатність)

**Інсайти:**
- Потрібні глибокі features (ResNet50)
- Full fine-tuning може покращити результат
- Найскладніший датасет серед CIFAR-10

### Взуття (Fashion-MNIST) 👟

**Складність:** Низька-Середня

**Найкраща модель:** Будь-яка (всі показують схожі результати)  
**Очікуваний Accuracy:** 90-95%

**Типові помилки:**
- Кросівки ↔ Черевики (схожі силуети)

**Інсайти:**
- Простий датасет, граyscale
- Frozen strategy достатньо
- Навіть MobileNetV2 дає 90%+

### Люди (CIFAR-100) 👨👩

**Складність:** Дуже висока

**Найкраща модель:** ResNet50 / EfficientNetB0  
**Очікуваний Accuracy:** 40-60%

**Типові помилки:**
- Хлопчик ↔ Чоловік (вік важко визначити)
- Дівчинка ↔ Жінка (вік важко визначити)
- Всі класи схожі (обличчя на 32×32 дуже малі)

**Інсайти:**
- Дуже мало даних (~500 на клас)
- Низька роздільна здатність
- Потрібен full fine-tuning
- Потенційно потрібна data augmentation

## Порівняльна таблиця (приблизні результати)

### Accuracy

| Датасет | VGG16 | ResNet50 | MobileNetV2 | EfficientNetB0 |
|---------|-------|----------|-------------|----------------|
| **Транспорт** | 0.87 | **0.91** | 0.88 | **0.90** |
| **Тварини** | 0.78 | **0.83** | 0.76 | 0.81 |
| **Взуття** | 0.92 | 0.94 | 0.91 | **0.94** |
| **Люди** | 0.48 | **0.57** | 0.45 | **0.55** |

### F1-Score

| Датасет | VGG16 | ResNet50 | MobileNetV2 | EfficientNetB0 |
|---------|-------|----------|-------------|----------------|
| **Транспорт** | 0.86 | **0.90** | 0.87 | **0.89** |
| **Тварини** | 0.77 | **0.82** | 0.75 | 0.80 |
| **Взуття** | 0.92 | 0.94 | 0.91 | **0.93** |
| **Люди** | 0.45 | **0.54** | 0.42 | **0.52** |

### Час навчання (GPU)

| Модель | Середній час на датасет |
|--------|-------------------------|
| **VGG16** | 8-10 хв |
| **ResNet50** | 6-8 хв |
| **MobileNetV2** | **4-5 хв** |
| **EfficientNetB0** | 5-7 хв |

## Висновки

### 🏆 Загальні рекомендації:

**Для максимальної якості:**
- Використовуйте **ResNet50** або **EfficientNetB0**
- Застосовуйте **partial** fine-tuning (80% frozen)
- Навчайте 15-20 epochs з early stopping
- Використовуйте data augmentation

**Для балансу якості/швидкості:**
- Використовуйте **MobileNetV2** або **EfficientNetB0**
- Застосовуйте **frozen** або **partial** fine-tuning
- Достатньо 10-15 epochs

**Для обмежених ресурсів:**
- Використовуйте **MobileNetV2**
- Застосовуйте **frozen** fine-tuning
- Навчайте 5-10 epochs

### 📊 Ключові інсайти:

1. **ResNet50** — найкраща якість на складних датасетах
2. **EfficientNetB0** — найкращий баланс (точність/швидкість/розмір)
3. **MobileNetV2** — найшвидша, гарна для production
4. **VGG16** — стабільна, але застаріла

5. **Partial fine-tuning** — оптимальна стратегія для більшості задач
6. **Frozen** працює добре для простих/схожих на ImageNet датасетів
7. **Full** потрібен тільки для дуже специфічних датасетів

8. **Confusion matrices** показують конкретні проблемні пари класів
9. **Аналіз помилок** допомагає зрозуміти слабкі місця моделі
10. **Візуалізація оптимізації** демонструє процес навчання

## Поширені питання

**Q: Яку стратегію fine-tuning обрати?**  
A: Для більшості задач — **partial**. Frozen якщо мало даних або обмежений час. Full якщо датасет дуже специфічний.

**Q: Чому ResNet50 краща за VGG16?**  
A: Skip connections дозволяють навчати глибші мережі, кращі features, ефективніша архітектура.

**Q: Навіщо аналізувати помилки класифікації?**  
A: Щоб зрозуміти які класи найскладніші, чи потрібні додаткові дані, чи є проблеми з датасетом.

**Q: Як покращити результати на датасеті "Люди"?**  
A: 
- Data augmentation (flips, rotation, color jitter)
- Більше даних
- Вища роздільна здатність зображень
- Спеціалізована модель для обличь (FaceNet, ArcFace)

**Q: Чому confusion matrix нормалізована по рядках?**  
A: Щоб показати частку передбачень для кожного справжнього класу, незалежно від кількості зразків.

**Q: Що робити якщо модель overfits?**  
A:
- Збільшити dropout
- Додати data augmentation
- Зменшити кількість trainable шарів (frozen strategy)
- Ранній зупинка (early stopping)
- Regularization (L2)

## Подальші покращення

Можливі розширення:

- 📊 **Data Augmentation**: обертання, відображення, color jitter
- 🎯 **Class weighting**: для незбалансованих датасетів
- 🔍 **Grad-CAM**: візуалізація що модель "бачить"
- 📈 **Learning rate scheduling**: cosine annealing, warm restarts
- 🧠 **Ensemble**: комбінування кількох моделей
- 🎨 **Більше датасетів**: STL-10, Caltech-101, Food-101
- ⚡ **Quantization**: зменшення розміру моделі
- 📱 **Mobile deployment**: TFLite, ONNX

## Технічні деталі

### Гіперпараметри:

**Frozen strategy:**
- Learning rate: 1e-3
- Optimizer: Adam
- Epochs: 20 (early stopping patience=5)

**Partial strategy:**
- Learning rate: 1e-4
- Optimizer: Adam
- Epochs: 20 (early stopping patience=5)
- Trainable layers: останні 20%

**Full strategy:**
- Learning rate: 1e-5
- Optimizer: Adam
- Epochs: 20 (early stopping patience=5)
- Trainable: всі шари

### Callbacks:

- **EarlyStopping**: monitor='val_loss', patience=5
- **ReduceLROnPlateau**: factor=0.5, patience=3
- **TrainingMonitor**: власний callback для відстеження ваг

### Архітектура Classifier Head:

```python
GlobalAveragePooling2D()
Dense(256, activation='relu')
Dropout(0.5)
Dense(128, activation='relu')
Dropout(0.3)
Dense(n_classes, activation='softmax')
```

## Автор

Проєкт створено для навчальних цілей в рамках курсу з глибокого навчання (Fine-Tuning).

## Ліцензія

Навчальний проєкт

---

**Примітка:** При першому запуску відбудеться завантаження pretrained моделей (~300MB). Датасети також завантажуються автоматично.

**🔥 Порада:** Якщо у вас є GPU (RTX 2060+), навчання буде в 4-5 разів швидше!

**💡 Важливо:** Результати можуть варіюватися через випадковість ініціалізації та data shuffling. Для відтворюваності встановлено random seed=42.

