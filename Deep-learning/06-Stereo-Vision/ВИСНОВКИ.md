# 📋 Висновки: Stereo Vision

## Лабораторна робота №6: Обчислення карти глибини

---

## 🎯 Мета дослідження

Порівняти чотири методи обчислення карти диспаратності (disparity map) для стерео-зору:
1. **Block Matching (BM)** — 25%
2. **Semi-Global Block Matching (SGBM)** — 25%
3. **SIFT Feature Matching** — 25%
4. **Template Matching** — 25%

---

## 📊 Експериментальні результати

### Порівняння швидкості методів

На основі файлу `methods_comparison.csv`:

| Метод | Час виконання | Відносна швидкість |
|-------|---------------|-------------------|
| **Block Matching (BM)** | **6.5 ms** | ⚡⚡⚡ Найшвидший |
| **SGBM** | 46.3 ms | ⚡⚡ Середній |
| **Template Matching** | 60.6 ms | ⚡ Повільний |
| **SIFT** | 71.9 ms | ⚡ Повільний |

### Графік порівняння швидкості

```
Час (мс)
 80 ┤
 72 ┤                        ████ SIFT
 64 ┤
 61 ┤              ████████████ Template
 56 ┤
 48 ┤
 46 ┤    ██████████ SGBM
 40 ┤
 32 ┤
 24 ┤
 16 ┤
  7 ┤ ██ BM
  0 ┼────────────────────────────────────
        BM    SGBM   Template   SIFT
```

**Висновок:** BM у **7 разів швидший** за SGBM та у **11 разів швидший** за SIFT.

---

## 🔬 Детальний аналіз методів

### 1. Block Matching (BM) — 25%

#### Результати
- ✅ **Час обчислення: 6.5 мс** — найшвидший метод
- ✅ Щільна карта диспаратності з чітким виділенням об'єктів
- ⚠️ Є артефакти всередині великих об'єктів ("дірки")
- ⚠️ Межі об'єктів менш рівні порівняно з SGBM

#### Дослідження параметрів (bm_parameters.csv)

| num_disparities | block_size | Час (мс) | Покриття (%) |
|-----------------|------------|----------|--------------|
| 16 | 5 | 5.0 | **76.9%** |
| 16 | 15 | 5.0 | 76.8% |
| 32 | 5 | 3.5 | 76.2% |
| 64 | 15 | 4.0 | 75.0% |
| 128 | 15 | 5.0 | 66.0% |

**Спостереження:**
- Найкраще покриття (**76.9%**) при `num_disparities=16`, `block_size=5`
- Збільшення `num_disparities` до 128 **знижує покриття до 64-66%**
- Час обчислення майже не залежить від параметрів (3-6 мс)
- Оптимальний `block_size` = 5-15 пікселів

#### Рекомендовані параметри
```python
numDisparities = 64   # Баланс якості та покриття
blockSize = 15        # Достатньо для згладжування шуму
```

---

### 2. Semi-Global Block Matching (SGBM) — 25%

#### Результати
- ✅ **Час обчислення: 46.3 мс** — у 7 разів повільніше за BM
- ✅ **Найкраща якість** карти диспаратності
- ✅ Однорідне заповнення всередині об'єктів (без "дірок")
- ✅ Плавніші межі об'єктів
- ✅ Коректне розділення об'єктів на різних глибинах

#### Порівняння з BM (візуальний аналіз)

| Критерій | BM | SGBM |
|----------|-----|------|
| Однорідність | Є дірки | ✅ Суцільна карта |
| Межі об'єктів | Нерівні | ✅ Плавні |
| Шум | Присутній | ✅ Мінімальний |
| Деталізація | Середня | ✅ Висока |

**Висновок:** SGBM дає **значно кращу якість** за рахунок глобальної оптимізації, але потребує більше часу.

---

### 3. SIFT Feature Matching — 25%

#### Результати
- ⚠️ **Час обчислення: 71.9 мс** — найповільніший
- ✅ Знайдено **279 точок відповідності**
- ✅ Середня диспаратність: **9.5 пікселів**
- ❌ **Розріджена карта** — тільки в ключових точках
- ❌ Не дає щільної карти глибини без додаткової інтерполяції

#### Розподіл диспаратності

```
Кількість точок
 250 ┤ ████
     │ ████
     │ ████
  50 ┤ ████
   0 ┼────────────────────────────────
      0   10   20   30   40   50   60   70
                  Disparity (пікселі)
```

**Спостереження:**
- Більшість точок мають низьку диспаратність (~5-15 пікселів)
- Це відповідає фоновим точкам (далекі об'єкти)
- Окремі кластери при 40-50 пікселів — близькі об'єкти

#### Переваги методу
- ✅ Робастність до освітлення
- ✅ Працює з великими базовими лініями
- ✅ Корисний для калібрування камер

#### Обмеження
- ❌ Потребує інтерполяції для щільної карти
- ❌ Повільний для великих зображень
- ❌ Залежить від наявності текстури

---

### 4. Template Matching — 25%

#### Результати
- ⚠️ **Час обчислення: 60.6 мс**
- ❌ На синтетичних даних **не показав хороших результатів**
- ❌ Рівномірний колір на виході — алгоритм не знайшов диспаратність

#### Причини низької якості
1. **Великий крок (step=8)** — втрата деталей
2. **Синтетичні дані** — низька текстурованість
3. **Локальний метод** — чутливий до шуму

#### Коли метод працює добре
- Реальні зображення з багатою текстурою
- Невеликі зображення
- Малі діапазони пошуку

---

## 📈 Зведена таблиця результатів

| Метод | Час (мс) | Якість | Щільність | Рекомендація |
|-------|----------|--------|-----------|--------------|
| **BM** | 6.5 | ⭐⭐⭐ | ✅ Висока | Real-time |
| **SGBM** | 46.3 | ⭐⭐⭐⭐⭐ | ✅ Висока | **Оптимальний** |
| **Template** | 60.6 | ⭐ | ❌ Низька | Не рекомендується |
| **SIFT** | 71.9 | ⭐⭐⭐⭐ | ❌ Розріджена | Спец. задачі |

---

## 🎓 Отримані знання

### Теоретичні
1. **Принцип стерео-зору** — використання паралаксу для визначення глибини
2. **Epipolar constraint** — обмеження пошуку вздовж горизонталі
3. **Компроміс швидкість/якість** — вибір методу залежить від задачі
4. **Глобальна vs локальна оптимізація** — SGBM vs BM

### Практичні
1. **BM** — ідеальний для real-time (робототехніка, дрони)
2. **SGBM** — найкраща якість для 3D реконструкції
3. **SIFT** — калібрування та wide-baseline stereo
4. **Параметри критично важливі** — numDisparities, blockSize

---

## 🏆 Рекомендації

### Для різних сценаріїв

| Сценарій | Рекомендований метод | Обґрунтування |
|----------|---------------------|---------------|
| **Автономний транспорт** | BM або SGBM | Швидкість + якість |
| **3D сканування** | SGBM | Максимальна якість |
| **Мобільні додатки** | BM | Мінімальні ресурси |
| **Наукові дослідження** | SGBM + SIFT | Повнота аналізу |
| **Калібрування камер** | SIFT | Точні відповідності |

### Оптимальна конфігурація

```python
# Для більшості задач
stereo = cv2.StereoSGBM_create(
    minDisparity=0,
    numDisparities=64,      # Кратне 16
    blockSize=5,            # Менший = більше деталей
    P1=8 * 3 * 5**2,       # Штраф за малі зміни
    P2=32 * 3 * 5**2,      # Штраф за великі зміни
    mode=cv2.STEREO_SGBM_MODE_SGBM
)
```

---

## ✅ Загальний висновок

Лабораторна робота продемонструвала порівняння чотирьох методів обчислення карти глибини:

1. **Block Matching (BM)** — найшвидший метод (**6.5 мс**), підходить для real-time застосувань
2. **SGBM** — **найкраща якість** при прийнятній швидкості (46.3 мс), рекомендується для більшості задач
3. **SIFT** — дає точні відповідності для **279 точок**, але не формує щільну карту
4. **Template Matching** — на синтетичних даних показав найгірші результати

**Головний висновок:** Для практичних задач стерео-зору рекомендується використовувати **SGBM** як основний метод, з можливим переходом на **BM** для real-time вимог.

---

## 📁 Створені файли

| Файл | Опис |
|------|------|
| `stereo_pair.png` | Візуалізація стерео-пари |
| `bm_vs_sgbm.png` | Порівняння BM та SGBM |
| `bm_parameters.csv` | Дослідження параметрів BM |
| `sift_matches.png` | Візуалізація SIFT зіставлень |
| `all_methods_comparison.png` | Порівняння всіх методів |
| `methods_comparison.csv` | Таблиця часу виконання |
| `3d_reconstruction.png` | 3D реконструкція сцени |

---

*Дата виконання: Грудень 2025*
