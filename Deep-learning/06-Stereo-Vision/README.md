# 🔬 Лабораторна робота №6: Stereo Vision

## Обчислення карти глибини (Disparity Map)

Ця лабораторна робота присвячена методам стерео-зору для обчислення карти глибини зображення на основі стерео-пари (лівого та правого зображень).

---

## 📋 Зміст

1. [Block Matching (BM)](#1-block-matching-bm---25) - 25%
2. [Semi-Global Block Matching (SGBM)](#2-semi-global-block-matching-sgbm---25) - 25%
3. [SIFT Feature Matching](#3-sift-feature-matching---25) - 25%
4. [Template Matching](#4-template-matching---25) - 25%

---

## 🎯 Мета роботи

- Вивчити принципи роботи стерео-зору
- Порівняти різні алгоритми обчислення карти диспаратності
- Зрозуміти переваги та недоліки кожного методу
- Виконати 3D реконструкцію сцени

---

## 📦 Встановлення

```bash
pip install -r requirements.txt
```

---

## 🚀 Запуск

```bash
python main.py
```

Після запуску оберіть пункт меню:
- **1** - Завантажити стерео-пару
- **2** - Block Matching
- **3** - SGBM
- **4** - SIFT
- **5** - Template Matching
- **6** - Порівняння всіх методів
- **7** - 3D реконструкція
- **8** - Виконати все

---

## 1. Block Matching (BM) - 25%

### Принцип роботи

Block Matching — це базовий алгоритм для обчислення disparity map:

1. **Вибір блоку**: Для кожного пікселя в лівому зображенні виділяється блок (вікно) фіксованого розміру
2. **Пошук відповідності**: Блок порівнюється з блоками в правому зображенні вздовж горизонтальної лінії
3. **Метрика схожості**: Sum of Absolute Differences (SAD)
4. **Disparity**: Зміщення, при якому блоки найбільш схожі

### Параметри

| Параметр | Опис | Типове значення |
|----------|------|-----------------|
| `numDisparities` | Максимальний діапазон пошуку (кратний 16) | 64 |
| `blockSize` | Розмір блоку (непарне число) | 15 |

### Переваги
- ✅ Швидкий
- ✅ Простий у реалізації
- ✅ Низькі вимоги до ресурсів

### Недоліки
- ❌ Шум на слабо-текстурованих ділянках
- ❌ Локальний метод (не враховує глобальну структуру)

---

## 2. Semi-Global Block Matching (SGBM) - 25%

### Принцип роботи

SGBM — покращений алгоритм, що враховує глобальну консистентність:

1. **Локальне порівняння**: Як у BM
2. **Глобальна оптимізація**: Мінімізація енергетичної функції вздовж 8-16 напрямків
3. **Динамічне програмування**: Оптимальний шлях з урахуванням штрафів за зміну disparity

### Енергетична функція

```
E(D) = Σ C(p, Dp) + Σ P1·T[|Dp - Dq| = 1] + Σ P2·T[|Dp - Dq| > 1]
```

де:
- `C(p, Dp)` — вартість зіставлення пікселя p з disparity Dp
- `P1` — штраф за малі зміни disparity
- `P2` — штраф за великі зміни disparity

### Переваги
- ✅ Краща якість на слабо-текстурованих ділянках
- ✅ Плавніші межі об'єктів
- ✅ Глобальна консистентність

### Недоліки
- ❌ Повільніший за BM
- ❌ Більше пам'яті

---

## 3. SIFT Feature Matching - 25%

### Принцип роботи

SIFT (Scale-Invariant Feature Transform) — алгоритм детекції ключових точок:

1. **Детекція ключових точок**: Знаходження характерних точок у обох зображеннях
2. **Обчислення дескрипторів**: 128-вимірний вектор для кожної точки
3. **Зіставлення**: Пошук відповідних точок за схожістю дескрипторів
4. **Фільтрація**: Epipolar constraint (y-координати мають співпадати)
5. **Disparity**: Різниця x-координат зіставлених точок

### Epipolar Constraint

Для ректифікованої стерео-пари відповідні точки лежать на одній горизонтальній лінії:
```
|y_left - y_right| < threshold
```

### Переваги
- ✅ Інваріантність до масштабу та повороту
- ✅ Робастність до освітлення
- ✅ Добре працює з великими зміщеннями

### Недоліки
- ❌ Розріджена карта (тільки в ключових точках)
- ❌ Може бути повільним
- ❌ Потребує інтерполяції для щільної карти

---

## 4. Template Matching - 25%

### Принцип роботи

Template Matching — пряме порівняння шаблонів:

1. **Вирізання шаблону**: Блок навколо кожного пікселя лівого зображення
2. **Sliding window**: Пошук шаблону в правому зображенні
3. **Метрика**: Normalized Cross-Correlation (NCC)
4. **Disparity**: Позиція найкращого співпадіння

### Normalized Cross-Correlation

```
NCC = Σ(I₁ - μ₁)(I₂ - μ₂) / (σ₁ · σ₂)
```

де μ — середнє, σ — стандартне відхилення

### Переваги
- ✅ Простий для розуміння
- ✅ Інваріантний до яскравості (з NCC)
- ✅ Гнучкі параметри

### Недоліки
- ❌ Дуже повільний
- ❌ Потребує кроку (step) для прискорення
- ❌ Залежить від розміру шаблону

---

## 📊 Порівняння методів

| Метод | Швидкість | Якість | Щільність | Використання |
|-------|-----------|--------|-----------|--------------|
| **BM** | ⚡⚡⚡ Висока | ⭐⭐ Середня | Щільна | Real-time |
| **SGBM** | ⚡⚡ Середня | ⭐⭐⭐ Висока | Щільна | Оптимальний вибір |
| **SIFT** | ⚡ Низька | ⭐⭐⭐ Висока* | Розріджена | Великі зміщення |
| **Template** | ⚡ Низька | ⭐⭐ Середня | Розріджена | Навчальні цілі |

*для знайдених точок

---

## 🧮 Формула глибини

Глибина точки обчислюється за формулою:

```
Z = (f × B) / d
```

де:
- `Z` — глибина (відстань до об'єкта)
- `f` — фокусна відстань камери
- `B` — базова лінія (відстань між камерами)
- `d` — диспаратність (disparity)

---

## 📁 Структура проекту

```
06-Stereo-Vision/
├── main.py              # Головний скрипт
├── requirements.txt     # Залежності
├── README.md           # Документація
├── ВИСНОВКИ.md         # Висновки
├── data/               # Стерео-пари зображень
│   ├── left.ppm
│   ├── right.ppm
│   └── ground_truth_disparity.npy
└── results/            # Результати
    ├── stereo_pair.png
    ├── bm_vs_sgbm.png
    ├── sift_matches.png
    ├── all_methods_comparison.png
    ├── methods_comparison.csv
    └── 3d_reconstruction.png
```

---

## 📚 Теоретичні основи

### Стерео-зір

Стерео-зір імітує людське бачення двома очима:
- Два зображення з різних точок зору
- Різниця в положенні об'єктів (parallax)
- Ближчі об'єкти мають більший parallax

### Disparity vs Depth

- **Disparity** — горизонтальне зміщення між лівим та правим зображенням
- **Depth** — реальна відстань до об'єкта
- Зв'язок: обернено пропорційний (більший disparity = менша глибина)

### Ректифікація

Для спрощення пошуку відповідностей зображення вирівнюються так, щоб:
- Epipolar lines були горизонтальними
- Пошук відбувався лише вздовж рядків

---

## 🔗 Корисні посилання

- [OpenCV Stereo Matching](https://docs.opencv.org/4.x/d2/d85/classcv_1_1StereoSGBM.html)
- [Middlebury Stereo Dataset](https://vision.middlebury.edu/stereo/)
- [SIFT Algorithm](https://en.wikipedia.org/wiki/Scale-invariant_feature_transform)
- [Stereo Vision Tutorial](https://learnopencv.com/depth-perception-using-stereo-camera-python-c/)

---

## 👨‍💻 Автор

Лабораторна робота з курсу "Глибинне навчання"

