# Object Detection (Детектування об'єктів)

## Опис проєкту

Цей проєкт демонструє комплексну роботу з детектуванням об'єктів (Object Detection) з використанням глибокого навчання. Проєкт включає **тестування pretrained моделей**, **оцінку точності** та **live детекцію з веб-камери**.

## Особливості проєкту

✨ **Основні можливості:**
- 🖼️ Детекція 80 класів COCO об'єктів
- 🧠 Підтримка multiple pretrained моделей
- 📊 Оцінка точності (Precision, Recall, IoU, mAP)
- 📹 Live детекція з веб-камери в реальному часі
- ⚡ Порівняння швидкості різних моделей
- 🎯 Візуалізація bounding boxes з confidence scores
- 💾 Збереження результатів та метрик

## Підтримувані об'єкти

Проєкт використовує моделі, навчені на датасеті COCO (80 класів):

### 👥 Люди та тварини:
- **person** - людина
- **cat, dog, bird** - домашні тварини
- **horse, cow, sheep, elephant, bear, zebra, giraffe** - великі тварини

### 🚗 Транспорт:
- **car** - автомобіль
- **bicycle** - велосипед
- **motorcycle** - мотоцикл
- **bus, train, truck** - великий транспорт
- **airplane, boat** - літак, човен

### 🏠 Побутові об'єкти:
- **chair, couch, bed** - меблі
- **bottle, cup, bowl** - посуд
- **tv, laptop, keyboard, mouse** - електроніка
- **book, clock, vase** - предмети інтер'єру

### Повний список: 80 класів COCO dataset

## Моделі

### 1. SSD MobileNet V2 ⚡ (Рекомендовано для live)

**Характеристики:**
- **Архітектура**: Single Shot MultiBox Detector + MobileNetV2 backbone
- **Розмір**: ~20MB
- **Швидкість**: ~30-50 FPS (CPU), ~100+ FPS (GPU)
- **mAP (COCO)**: ~22%

**Особливості:**
- ✅ Найшвидша модель
- ✅ Низьке споживання пам'яті
- ✅ Ідеальна для real-time
- ⚠️ Нижча точність ніж у Faster R-CNN

**Коли використовувати:**
- Live детекція з веб-камери
- Мобільні пристрої
- Обмежені ресурси
- Потрібна швидкість

### 2. EfficientDet D0 🎯 (Баланс)

**Характеристики:**
- **Архітектура**: EfficientNet backbone + BiFPN
- **Розмір**: ~16MB
- **Швидкість**: ~20-30 FPS (CPU), ~80+ FPS (GPU)
- **mAP (COCO)**: ~33.8%

**Особливості:**
- ✅ Гарний баланс швидкості/точності
- ✅ Компактна модель
- ✅ Сучасна архітектура
- ✅ Краща точність ніж SSD

**Коли використовувати:**
- Потрібен баланс швидкості та якості
- Обмежена пам'ять
- Середній рівень точності

### 3. Faster R-CNN ResNet50 🏆 (Максимальна точність)

**Характеристики:**
- **Архітектура**: Region-based CNN + ResNet50 backbone
- **Розмір**: ~110MB
- **Швидкість**: ~5-10 FPS (CPU), ~30-40 FPS (GPU)
- **mAP (COCO)**: ~29-32%

**Особливості:**
- ✅ Висока точність
- ✅ Стабільні результати
- ✅ Добре виявляє малі об'єкти
- ⚠️ Повільніша за інші

**Коли використовувати:**
- Потрібна максимальна точність
- Є GPU
- Офлайн обробка
- Критична якість детекції

### 4. CenterNet ResNet50 🎨

**Характеристики:**
- **Архітектура**: Anchor-free detection + ResNet50
- **Розмір**: ~70MB
- **Швидкість**: ~15-25 FPS (CPU), ~60+ FPS (GPU)
- **mAP (COCO)**: ~28-31%

**Особливості:**
- ✅ Anchor-free підхід
- ✅ Хороша точність
- ✅ Краще виявляє перекриваючі об'єкти
- ✅ Середня швидкість

**Коли використовувати:**
- Багато перекриваючих об'єктів
- Потрібна точність центрів об'єктів
- Середні ресурси

## Метрики оцінки

### 1. IoU (Intersection over Union) 📐

**Що це:**
- Вимірює перекриття між predicted та ground truth box
- Діапазон: [0, 1], де 1 = ідеальне співпадіння

**Формула:**
```
IoU = Area of Overlap / Area of Union
```

**Інтерпретація:**
- IoU > 0.7: Відмінно ✅✅
- IoU 0.5-0.7: Добре ✅
- IoU 0.3-0.5: Середньо ⚠️
- IoU < 0.3: Погано ❌

**Використання:**
- Стандартний поріг: 0.5 (COCO використовує 0.5-0.95)

### 2. Precision (Точність) 🎯

**Що це:**
- Скільки з predicted boxes насправді правильні

**Формула:**
```
Precision = TP / (TP + FP)
```

**Де:**
- TP (True Positives) = правильні детекції
- FP (False Positives) = помилкові детекції

**Інтерпретація:**
- Precision > 0.8: Відмінно ✅✅
- Precision 0.6-0.8: Добре ✅
- Precision 0.4-0.6: Середньо ⚠️
- Precision < 0.4: Погано ❌

### 3. Recall (Повнота) 🔍

**Що це:**
- Скільки справжніх об'єктів було знайдено

**Формула:**
```
Recall = TP / (TP + FN)
```

**Де:**
- FN (False Negatives) = пропущені об'єкти

**Інтерпретація:**
- Recall > 0.8: Відмінно ✅✅
- Recall 0.6-0.8: Добре ✅
- Recall 0.4-0.6: Середньо ⚠️
- Recall < 0.4: Погано ❌

### 4. F1-Score ⚖️

**Що це:**
- Гармонічне середнє Precision та Recall

**Формула:**
```
F1 = 2 × (Precision × Recall) / (Precision + Recall)
```

**Інтерпретація:**
- F1 > 0.8: Відмінний баланс ✅✅
- F1 0.6-0.8: Гарний баланс ✅
- F1 < 0.6: Потрібне покращення ⚠️

### 5. mAP (mean Average Precision) 📊

**Що це:**
- Стандартна метрика для object detection
- Середнє значення Average Precision по всіх класах

**COCO mAP:**
- Використовує IoU пороги від 0.5 до 0.95 (крок 0.05)
- mAP@0.5 = середнє AP при IoU=0.5
- mAP@0.5:0.95 = середнє по всіх порогах

**Інтерпретація (COCO):**
- mAP > 0.5: State-of-the-art ✅✅
- mAP 0.3-0.5: Добре ✅
- mAP 0.2-0.3: Середньо ⚠️
- mAP < 0.2: Слабо ❌

### 6. FPS (Frames Per Second) ⚡

**Що це:**
- Кількість кадрів, оброблених за секунду
- Критична метрика для real-time додатків

**Вимоги:**
- > 30 FPS: Real-time ✅✅
- 15-30 FPS: Прийнятно для live ✅
- < 15 FPS: Повільно ⚠️

## Структура проєкту

```
Deep-learning/04-Object-detection/
│
├── main.py                           # Головний скрипт
├── requirements.txt                  # Залежності
├── README.md                         # Документація
├── run.bat                          # Швидкий запуск (Windows)
│
├── data/                            # Тестові дані
│   ├── test_image_1.jpg
│   ├── test_image_2.jpg
│   └── ...
│
├── models/                          # Завантажені моделі (кеш)
│   └── ...
│
└── results/                         # Згенеровані файли
    ├── detections_ssd_mobilenet_v2.png      # Детекції SSD
    ├── detections_efficientdet_d0.png       # Детекції EfficientDet
    ├── models_benchmark.csv                  # Метрики моделей
    ├── models_comparison.png                 # Графік порівняння
    └── live_capture_*.jpg                    # Збережені кадри з live
```

## Встановлення

### Вимоги:
- Python 3.8+
- pip
- Веб-камера (для live детекції)
- GPU (опціонально, але рекомендується)

### Кроки встановлення:

```bash
# Перейти до директорії проєкту
cd Deep-learning/04-Object-detection

# (Опціонально) Створення віртуального середовища
python -m venv venv

# Активація віртуального середовища
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Встановлення залежностей
pip install -r requirements.txt
```

**Важливо:** Перше завантаження моделей з TensorFlow Hub може зайняти час (~100-500MB).

## Запуск

### Варіант 1: Інтерактивний режим

```bash
python main.py
```

Меню опцій:
1. **Тестування на зображеннях** - завантажує тестові зображення та виконує детекцію
2. **Бенчмарк моделей** - порівнює швидкість різних моделей
3. **Live детекція** - запускає детекцію з веб-камери
4. **Виконати все** - виконує всі дії послідовно

### Варіант 2: Швидкий запуск (Windows)

```bash
run.bat
```

### Варіант 3: Прямий запуск live детекції

Відредагуйте `main.py` та закоментуйте меню, залиште тільки:

```python
if __name__ == "__main__":
    live_detection(model_name='ssd_mobilenet_v2', confidence_threshold=0.5)
```

## Використання

### 1. Тестування на зображеннях

Скрипт автоматично:
1. 📥 Завантажить тестові зображення з TensorFlow models repo
2. 🧠 Завантажить pretrained модель
3. 🔍 Виконає детекцію об'єктів
4. 🎨 Намалює bounding boxes з мітками
5. 💾 Збереже результати у `results/`

**Приклад виведення:**
```
Знайдено 5 об'єктів:
  - person: 2x (avg score: 0.95)
  - car: 2x (avg score: 0.87)
  - bicycle: 1x (avg score: 0.78)
```

### 2. Бенчмарк моделей

Порівнює різні моделі за:
- ⚡ Швидкість (FPS, час інференції)
- 🎯 Кількість детекцій
- 📊 Якість результатів

Створює:
- `models_benchmark.csv` - таблиця з метриками
- `models_comparison.png` - графік порівняння

### 3. Live детекція з веб-камери

**Керування:**
- `q` або `ESC` - вихід з програми
- `s` - зберегти поточний кадр
- `+` або `=` - збільшити confidence threshold (+0.05)
- `-` або `_` - зменшити confidence threshold (-0.05)

**Що показується на екрані:**
- 🎯 Bounding boxes з мітками класів
- 📊 Confidence scores для кожної детекції
- ⚡ FPS (кадрів за секунду)
- 🔢 Кількість знайдених об'єктів
- ⏱️ Час інференції (ms)
- 🎚️ Поточний confidence threshold

**Приклад:**
```
FPS: 32.5
Detections: 3
Confidence: 0.50
Time: 31ms
```

### 4. Налаштування confidence threshold

**Що це:**
- Мінімальна впевненість моделі для показу детекції
- Діапазон: [0.0, 1.0]

**Рекомендації:**
- **0.3-0.4**: Більше детекцій, більше false positives
- **0.5**: Стандартне значення, гарний баланс ✅
- **0.7-0.8**: Менше детекцій, вища точність

## Результати та інтерпретація

### Очікувані результати (приклад):

#### SSD MobileNet V2:
- **FPS**: ~30-50 (CPU), ~100+ (GPU)
- **Точність**: Середня (mAP ~22%)
- **Використання**: Real-time додатки

#### EfficientDet D0:
- **FPS**: ~20-30 (CPU), ~80+ (GPU)
- **Точність**: Хороша (mAP ~34%)
- **Використання**: Баланс швидкості/якості

#### Faster R-CNN ResNet50:
- **FPS**: ~5-10 (CPU), ~30-40 (GPU)
- **Точність**: Висока (mAP ~32%)
- **Використання**: Офлайн обробка

### Порівняльна таблиця (приклад):

| Модель | Avg Time (ms) | Avg FPS | Avg Detections | mAP (COCO) |
|--------|--------------|---------|----------------|------------|
| **SSD MobileNet V2** | 20 | **50** | 8.5 | 22% |
| **EfficientDet D0** | 35 | 28 | 9.2 | **34%** |
| **Faster R-CNN** | 100 | 10 | 10.5 | 32% |

### Ключові висновки:

**🏆 Для максимальної точності:**
- EfficientDet D0 або Faster R-CNN ResNet50
- Використовуйте GPU
- Confidence threshold: 0.5-0.6

**⚡ Для максимальної швидкості:**
- SSD MobileNet V2
- Працює добре навіть на CPU
- Confidence threshold: 0.4-0.5

**⚖️ Для балансу:**
- EfficientDet D0
- Гарне співвідношення швидкості/якості
- Універсальний вибір

## Live детекція: що очікувати

### На CPU:
- **SSD MobileNet V2**: 20-30 FPS ✅
- **EfficientDet D0**: 10-15 FPS ⚠️
- **Faster R-CNN**: 3-5 FPS ❌

### На GPU:
- **SSD MobileNet V2**: 80-120 FPS ✅✅
- **EfficientDet D0**: 50-80 FPS ✅✅
- **Faster R-CNN**: 25-40 FPS ✅

### Типові сценарії:

**1. Детекція людей:**
- Працює дуже добре (клас 'person' добре навчений)
- Висока точність навіть на відстані
- Може виявляти multiple people

**2. Детекція автомобілів:**
- Відмінна точність на вулиці
- Розрізняє car, bus, truck, motorcycle
- Добре працює при різному освітленні

**3. Детекція обличь:**
- Використовує клас 'person' (не окреме виявлення обличчя)
- Для спеціалізованої детекції обличч краще використати face detection моделі

**4. Побутові об'єкти:**
- Добре виявляє великі об'єкти (laptop, tv, chair)
- Малі об'єкти (phone, cup) можуть бути складними

## Поширені питання

**Q: Чому FPS на моєму комп'ютері нижчий?**  
A: FPS залежить від:
- Процесора/GPU
- Розміру зображення з камери
- Інших запущених програм
- Моделі (SSD швидша за Faster R-CNN)

**Q: Як покращити точність детекції?**  
A:
1. Використайте кращу модель (Faster R-CNN, EfficientDet)
2. Збільшіть роздільну здатність камери
3. Покращіть освітлення
4. Зменште відстань до об'єктів
5. Використайте GPU

**Q: Модель не виявляє деякі об'єкти. Чому?**  
A:
- Об'єкт не входить в 80 класів COCO
- Занадто низький confidence threshold (спробуйте зменшити)
- Об'єкт частково закритий
- Погане освітлення
- Об'єкт занадто малий

**Q: Як виявити об'єкти, яких немає в COCO?**  
A: Потрібно:
1. Створити власний датасет
2. Донавчити (fine-tune) модель
3. Або навчити нову модель з нуля

**Q: Чому модель помиляється (false positives)?**  
A:
- Збільште confidence threshold
- Використайте точнішу модель
- Деякі об'єкти схожі візуально

**Q: Як прискорити детекцію?**  
A:
1. Використайте SSD MobileNet V2
2. Зменште роздільну здатність камери
3. Використайте GPU
4. Збільште confidence threshold
5. Оптимізуйте модель (TensorRT, ONNX)

**Q: Чи можна зберегти відео з детекціями?**  
A: Так, додайте `cv2.VideoWriter` в live_detection функцію:

```python
fourcc = cv2.VideoWriter_fourcc(*'XVID')
out = cv2.VideoWriter('output.avi', fourcc, 20.0, (640, 480))

# У циклі:
out.write(frame_with_boxes)

# Після циклу:
out.release()
```

## Розширення та покращення

Можливі розширення проєкту:

### 🎯 Покращення точності:
- Fine-tuning на власному датасеті
- Ensemble декількох моделей
- Post-processing (NMS налаштування)
- Test-time augmentation

### ⚡ Оптимізація швидкості:
- TensorRT оптимізація
- ONNX Runtime
- Quantization (INT8)
- Model pruning

### 📊 Додаткові метрики:
- Confusion matrix
- Per-class mAP
- Speed/accuracy trade-off curves
- Tracking metrics (для відео)

### 🎨 Візуалізація:
- Heatmaps attention maps
- Confidence histograms
- Object size distribution
- Temporal smoothing

### 🔍 Нові можливості:
- Object tracking (DeepSORT, ByteTrack)
- Multi-camera setup
- 3D bounding boxes
- Instance segmentation
- Pose estimation

### 📱 Deployment:
- Mobile deployment (TFLite)
- Web deployment (TensorFlow.js)
- Edge devices (NVIDIA Jetson)
- Cloud API (FastAPI, Flask)

## Технічні деталі

### SSD MobileNet V2:
- **Input**: 320×320 RGB
- **Backbone**: MobileNetV2
- **Head**: SSD detection head
- **Anchors**: 6 anchor boxes per location
- **NMS**: IoU threshold 0.5

### EfficientDet D0:
- **Input**: 512×512 RGB
- **Backbone**: EfficientNet-B0
- **FPN**: BiFPN (Bi-directional Feature Pyramid)
- **Compound scaling**: Depth, width, resolution

### Faster R-CNN ResNet50:
- **Input**: Variable size (resized to 640×640)
- **Backbone**: ResNet50
- **RPN**: Region Proposal Network
- **Two-stage**: RPN + R-CNN head

### Object Detection Pipeline:

```
1. Input Image
   ↓
2. Preprocessing (resize, normalize)
   ↓
3. Backbone Feature Extraction
   ↓
4. Detection Head (boxes + classes)
   ↓
5. Post-processing (NMS)
   ↓
6. Output: boxes, classes, scores
```

### Non-Maximum Suppression (NMS):

**Що це:**
- Видаляє дублікати детекцій
- Залишає box з найвищим confidence

**Алгоритм:**
1. Сортувати boxes по score
2. Взяти box з найвищим score
3. Видалити boxes з IoU > threshold (0.5)
4. Повторити для решти boxes

## Troubleshooting

### Проблема: Модель не завантажується

**Рішення:**
```bash
# Очистіть кеш TensorFlow Hub
rm -rf ~/.cache/tensorflow_hub/

# Перевірте інтернет з'єднання
# Спробуйте знову
```

### Проблема: Веб-камера не відкривається

**Рішення:**
```python
# Спробуйте інший індекс камери
cap = cv2.VideoCapture(1)  # Замість 0

# Перевірте доступні камери
for i in range(10):
    cap = cv2.VideoCapture(i)
    if cap.isOpened():
        print(f"Camera {i}: Available")
        cap.release()
```

### Проблема: Низький FPS

**Рішення:**
1. Зменште роздільну здатність:
```python
cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 480)
```

2. Використайте легшу модель (SSD MobileNet)
3. Збільште confidence threshold
4. Використайте GPU

### Проблема: CUDA errors (на GPU)

**Рішення:**
```bash
# Перевірте версію CUDA
nvcc --version

# Перевірте TensorFlow-GPU
python -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"

# Переустановіть TensorFlow з GPU підтримкою
pip install tensorflow[and-cuda]==2.17.0
```

## Автор

Проєкт створено для навчальних цілей в рамках курсу з глибокого навчання (Object Detection).

## Ліцензія

Навчальний проєкт

---

## Корисні ресурси

📚 **Документація:**
- [TensorFlow Object Detection API](https://github.com/tensorflow/models/tree/master/research/object_detection)
- [TensorFlow Hub Models](https://tfhub.dev/s?module-type=image-object-detection)
- [COCO Dataset](https://cocodataset.org/)

📝 **Papers:**
- SSD: [Single Shot MultiBox Detector](https://arxiv.org/abs/1512.02325)
- Faster R-CNN: [Towards Real-Time Object Detection](https://arxiv.org/abs/1506.01497)
- EfficientDet: [Scalable and Efficient Object Detection](https://arxiv.org/abs/1911.09070)

🎓 **Tutorials:**
- [TensorFlow Object Detection Tutorial](https://tensorflow-object-detection-api-tutorial.readthedocs.io/)
- [OpenCV Object Detection](https://docs.opencv.org/master/d5/d54/group__objdetect.html)

---

**Примітка:** При першому запуску моделі завантажуються з TensorFlow Hub (~100-500MB). Це може зайняти 5-15 хвилин в залежності від інтернету.

**🔥 Порада:** Для найкращих результатів live детекції використовуйте хороше освітлення та тримайте камеру стабільно!

**💡 Експериментуйте:** Спробуйте різні confidence thresholds, моделі та сценарії використання!

