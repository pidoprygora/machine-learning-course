# Feature Visualization - Візуалізація нейронних мереж

Лабораторна робота з візуалізації фільтрів згортки та шарів глибокого навчання.

## 📋 Завдання

| Компонент | Вага |
|-----------|------|
| Візуалізація фільтрів згортки (Convolution Filters) | 50% |
| Візуалізація шарів (Layer Activations) | 50% |

## 🎯 Мета роботи

Навчитися інтерпретувати та візуалізувати внутрішню роботу згорткових нейронних мереж:
- Розуміння, яких патернів навчаються фільтри на різних глибинах
- Аналіз активацій шарів при обробці зображень
- Порівняння власної та pre-trained моделі

## 🏗️ Архітектура

### Модель для аналізу: U-Net (з лаби 05-Texture-Segmentation)

```
Вхід (256×256×3)
    │
    ├─► enc_conv1_1 (32 фільтри 3×3)
    ├─► enc_conv1_2 (32 фільтри 3×3)
    ├─► MaxPool (128×128)
    │
    ├─► enc_conv2_1 (64 фільтри 3×3)
    ├─► enc_conv2_2 (64 фільтри 3×3)
    ├─► MaxPool (64×64)
    │
    ├─► bottleneck_conv1 (128 фільтрів 3×3)
    ├─► bottleneck_conv2 (128 фільтрів 3×3)
    │
    ├─► UpSample + Concat
    ├─► dec_conv1_1 (64 фільтри)
    ├─► dec_conv1_2 (64 фільтри)
    │
    ├─► UpSample + Concat
    ├─► dec_conv2_1 (32 фільтри)
    ├─► dec_conv2_2 (32 фільтри)
    │
    └─► output_conv (1 фільтр, сигмоїда)
```

## 📊 Візуалізація фільтрів згортки (50%)

### Що таке фільтри згортки?

Фільтри (або ядра) — це невеликі матриці ваг, які мережа навчається під час тренування. Кожен фільтр шукає певний патерн у вхідних даних.

### Що ми візуалізуємо:

1. **Фільтри кожного шару** (`filters_*.png`)
   - Відображення ваг як зображень
   - Для RGB входу — показуємо кольорові фільтри
   - Для багатоканальних — середнє по входах

2. **Порівняння фільтрів** (`filters_comparison.png`)
   - Еволюція складності від простих до складних
   - Перші шари: краї, градієнти
   - Глибші шари: текстури, форми

3. **Статистика фільтрів** (`filter_statistics.png`)
   - Розподіл ваг
   - Норми фільтрів
   - Середні значення по шарах

### Інтерпретація:

| Шар | Типові патерни |
|-----|----------------|
| Перший | Горизонтальні/вертикальні краї, градієнти кольорів |
| Середній | Текстури, кути, прості форми |
| Глибокий | Складні патерни, частини об'єктів |

## 🔥 Візуалізація шарів / активацій (50%)

### Що таке активації?

Активації (feature maps) — це результат застосування фільтрів до вхідних даних. Вони показують, що "бачить" мережа на кожному етапі обробки.

### Що ми візуалізуємо:

1. **Feature maps кожного шару** (`activations_*.png`)
   - Всі канали активацій
   - Візуалізація як карти відгуків

2. **Теплові карти** (`activation_heatmaps.png`)
   - Середня активація накладена на оригінал
   - Показує "фокус уваги" кожного шару

3. **Статистика активацій** (`activation_statistics.png`)
   - Середні значення, дисперсія
   - Спарсність (% нульових значень після ReLU)
   - "Мертві" канали

4. **Прогресія обробки** (`layer_progression.png`)
   - Як змінюється представлення від входу до виходу
   - Зменшення просторової розмірності
   - Збільшення семантичної абстракції

### Ключові спостереження:

- **Спарсність зростає** з глибиною (ReLU занулює багато активацій)
- **Просторова роздільність падає** (MaxPooling)
- **Семантичне багатство росте** (більше каналів)

## 🚀 Запуск

```bash
# Встановлення залежностей
pip install -r requirements.txt

# Запуск
python main.py

# Вибір опції:
# 1 - Тільки фільтри
# 2 - Тільки активації
# 3 - Pre-trained VGG16
# 4 - Повна лабораторна (рекомендовано)
```

## 📁 Структура результатів

```
results/
├── filters_enc_conv1_1.png       # Фільтри першого шару
├── filters_enc_conv1_2.png       # ...
├── filters_enc_conv2_1.png       
├── filters_*.png                 # Фільтри всіх згорткових шарів
├── filters_comparison.png        # Порівняння фільтрів
├── filter_statistics.png         # Статистика ваг
│
├── input_image_for_activations.png  # Вхідне зображення
├── activations_00_enc_conv1_1.png   # Активації першого шару
├── activations_*.png                # Активації всіх шарів
├── activation_heatmaps.png          # Теплові карти
├── activation_statistics.png        # Статистика активацій
├── layer_progression.png            # Прогресія через мережу
│
├── vgg16_filters_*.png              # Фільтри VGG16
└── vgg16_activations.png            # Активації VGG16
```

## 🔬 Додатково: Pre-trained VGG16

Для порівняння візуалізуємо фільтри та активації VGG16, навченої на ImageNet:
- Показує, яких патернів навчилася "професійна" модель
- Перший шар: детектори Габора, кольорові blob-и
- Глибокі шари: частини об'єктів, текстури

## 📚 Теоретичні основи

### Чому візуалізація важлива?

1. **Інтерпретованість** — розуміння, що робить модель
2. **Дебаг** — виявлення проблем (мертві нейрони, перенавчання)
3. **Довіра** — можливість пояснити рішення моделі

### Математика фільтрів

Згортка фільтра `K` з входом `I`:

```
(I * K)[x,y] = Σᵢ Σⱼ I[x+i, y+j] · K[i,j]
```

Результат — feature map, що показує "схожість" локальної області на патерн фільтра.

### ReLU та спарсність

```python
ReLU(x) = max(0, x)
```

Занулення негативних значень створює **спарсні** представлення, що покращує:
- Обчислювальну ефективність
- Інтерпретованість
- Узагальнення

## 👨‍🔬 Автор

Лабораторна робота з курсу "Глибоке навчання"

## 📖 Посилання

- [Visualizing and Understanding Convolutional Networks (Zeiler & Fergus, 2014)](https://arxiv.org/abs/1311.2901)
- [Deep Visualization Toolbox](https://github.com/yosinski/deep-visualization-toolbox)
- [Keras Documentation: Model Visualization](https://keras.io/guides/visualization/)

