# Бенчмаркінг екстракторів ознак (Feature Extraction)

## Опис проєкту

Цей проєкт проводить комплексний бенчмаркінг різних методів екстракції ознак з використанням глибокого навчання. Фокус на **візуалізації внутрішніх представлень CNN**, порівнянні якості екстракторів та аналізі separability класів через PCA та t-SNE.

## Особливості проєкту

✨ **Основні можливості:**
- 🖼️ Робота з 3 датасетами різної складності
- 🧠 5 різних feature extractors (власні + pretrained)
- 🔍 Візуалізація CNN фільтрів та активацій
- 📊 PCA та t-SNE проекції для всіх екстракторів
- 📈 Комплексна оцінка якості кластеризації
- ⚡ Порівняння швидкості екстракції
- 🎯 Бенчмарк на датасетах з різною separability

## Датасети

Проєкт використовує 3 класичні датасети з різним рівнем складності:

### 1. MNIST (легко розділити) ✅
- **Розмір**: 6000 train / 2000 test
- **Класи**: 10 цифр (0-9)
- **Розмір зображення**: 28×28 (grayscale)
- **Складність**: Низька - чіткі границі між класами
- **Опис**: Рукописні цифри з чіткими візуальними відмінностями

### 2. Fashion-MNIST (важко розділити) ⚠️
- **Розмір**: 6000 train / 2000 test
- **Класи**: 10 предметів одягу (футболка, штани, светр, плаття, пальто, сандалі, сорочка, кросівки, сумка, черевики)
- **Розмір зображення**: 28×28 (grayscale)
- **Складність**: Висока - класи перекриваються, схожі текстури
- **Опис**: Зображення одягу з магазину Zalando

### 3. CIFAR-10 (середня складність) 🎨
- **Розмір**: 5000 train / 1500 test
- **Класи**: 10 об'єктів (літак, автомобіль, птах, кіт, олень, собака, жаба, кінь, корабель, вантажівка)
- **Розмір зображення**: 32×32 (RGB)
- **Складність**: Середня - реальні фото з варіативними фонами
- **Опис**: Природні зображення з різних ракурсів

## Feature Extractors

### 1. Custom CNN (власний) 🛠️

**Архітектура:**
```
Input (28×28×1 або 32×32×3)
  ↓
Conv2D(32) → MaxPool → BatchNorm
  ↓
Conv2D(64) → MaxPool → BatchNorm
  ↓
Conv2D(128) → GlobalAveragePooling
  ↓
Dense(256) - Features
  ↓
Dense(10) - Softmax
```

**Особливості:**
- ✅ Легкий і швидкий
- ✅ Навчається end-to-end на конкретному датасеті
- ✅ 256-вимірний feature vector
- ✅ Ідеальний баланс швидкості та якості

**Переваги:**
- Адаптується під конкретну задачу
- Не потребує багато пам'яті
- Швидке навчання (5 epochs)

**Коли використовувати:**
- Маленькі/середні датасети
- Обмежені обчислювальні ресурси
- Потрібен контроль над архітектурою

### 2. Autoencoder (власний) 🔄

**Архітектура:**
```
Encoder:
  Input → Conv2D(32) → MaxPool → Conv2D(64) → MaxPool → Dense(128 latent)

Decoder:
  Dense → Reshape → Conv2DTranspose(64) → Conv2DTranspose(32) → Output
```

**Особливості:**
- ✅ Unsupervised навчання (не потребує міток)
- ✅ 128-вимірний latent space
- ✅ Вчиться стискати важливу інформацію
- ✅ Реконструкція оригінального зображення

**Переваги:**
- Працює без міток класів
- Знаходить компактне представлення
- Можна використати для denoising

**Коли використовувати:**
- Немає міток класів
- Потрібна компресія даних
- Аномалія detection

### 3. VGG16 (ImageNet pretrained) 🏆

**Параметри:**
- **Ваги**: ImageNet (1000 класів)
- **Архітектура**: 16 шарів (13 conv + 3 FC)
- **Features**: 512 після GlobalAveragePooling
- **Розмір**: ~14.7M параметрів

**Особливості:**
- ✅ Класична архітектура
- ✅ Глибоке розуміння візуальних ознак
- ✅ Стабільні та надійні features
- ⚠️ Повільніша за сучасні моделі

**Переваги:**
- Дуже добре навчена на ImageNet
- Проста та зрозуміла архітектура
- Високоякісні features

**Коли використовувати:**
- Потрібна висока якість
- Зображення схожі на ImageNet
- Є достатньо обчислювальних ресурсів

### 4. ResNet50 (ImageNet pretrained) 🚀

**Параметри:**
- **Ваги**: ImageNet (1000 класів)
- **Архітектура**: 50 шарів з residual connections
- **Features**: 2048 після GlobalAveragePooling
- **Розмір**: ~25.6M параметрів

**Особливості:**
- ✅ Революційна архітектура з skip connections
- ✅ Дуже глибока мережа без gradient vanishing
- ✅ Високоякісні features (2048-D)
- ✅ Швидша за VGG16

**Переваги:**
- Найкраща якість features
- Residual connections дозволяють глибину
- Ефективне навчання

**Коли використовувати:**
- Потрібна максимальна якість
- Складні візуальні задачі
- Transfer learning

### 5. MobileNetV2 (ImageNet pretrained) ⚡

**Параметри:**
- **Ваги**: ImageNet (1000 класів)
- **Архітектура**: Inverted residuals + linear bottlenecks
- **Features**: 1280 після GlobalAveragePooling
- **Розмір**: ~3.5M параметрів

**Особливості:**
- ✅ Оптимізована для мобільних пристроїв
- ✅ Найшвидша серед pretrained моделей
- ✅ Малий розмір моделі
- ✅ Депthwise separable convolutions

**Переваги:**
- Дуже швидка інференція
- Маленький розмір
- Гарний баланс швидкості/якості

**Коли використовувати:**
- Обмежені ресурси
- Потрібна швидкість
- Mobile/edge deployment

## Візуалізації

### 1. CNN Внутрішні представлення 🔍

#### Фільтри першого шару (conv1)
- Показує, що CNN вчиться розпізнавати:
  - Edges (краї)
  - Corners (кути)
  - Simple patterns (прості патерни)
  - Gradient detectors
- **Файл**: `cnn_filters_conv1.png`

#### Feature Maps (активації)
- Візуалізація активацій для конкретного зображення
- Показує прогресію від простих до складних ознак
- Шари: conv1 → conv2 → conv3
- **Файл**: `cnn_activations_progression.png`

#### Детальні Feature Maps
- 32 окремі карти активацій першого шару
- Показує що кожен фільтр "бачить"
- **Файл**: `cnn_feature_maps_conv1.png`

### 2. PCA Проекції 📊

**Principal Component Analysis** - лінійне зменшення розмірності

**Що показує:**
- Лінійна структура даних
- Основні напрямки варіації
- Explained variance (скільки інформації збережено)

**Переваги:**
- ✅ Швидко обчислюється
- ✅ Детермінований результат
- ✅ Можна інтерпретувати компоненти

**Файл**: `pca_projections_*.png`

### 3. t-SNE Проекції 🎨

**t-Distributed Stochastic Neighbor Embedding** - нелінійне зменшення розмірності

**Що показує:**
- Нелінійна структура даних
- Локальні групування (кластери)
- Separability класів

**Переваги:**
- ✅ Знаходить складні структури
- ✅ Краща візуалізація кластерів
- ✅ Популярний у deep learning

**Недоліки:**
- ⚠️ Повільний на великих датасетах
- ⚠️ Недетермінований (кожен запуск різний)
- ⚠️ Важко інтерпретувати осі

**Файл**: `tsne_projections_*.png`

## Метрики оцінки

Усі метрики обчислюються після кластеризації features за допомогою K-Means.

### Внутрішні метрики (не потребують справжніх міток):

**1. Silhouette Score** (вище = краще, діапазон: [-1, 1])
- Вимірює наскільки добре кожна точка вписується у свій кластер
- **Формула**: (b - a) / max(a, b)
  - a = середня відстань до точок свого кластера
  - b = середня відстань до найближчого іншого кластера
- **Інтерпретація**:
  - 0.71-1.0: Сильна структура ✅
  - 0.51-0.70: Середня структура ⚠️
  - 0.26-0.50: Слабка структура ❌
  - < 0.25: Кластери відсутні ❌❌

**2. Davies-Bouldin Index** (нижче = краще, діапазон: [0, ∞))
- Середнє співвідношення внутрішньокластерної та міжкластерної відстані
- **Чому важливо**: показує компактність та відокремленість кластерів
- **Добре значення**: < 1.0

**3. Calinski-Harabasz Index** (вище = краще)
- Відношення міжкластерної дисперсії до внутрішньокластерної
- **Чому важливо**: показує чіткість розділення кластерів
- **Добре значення**: > 100

### Зовнішні метрики (потребують справжні мітки):

**4. Adjusted Rand Index (ARI)** (діапазон: [-1, 1], 1 = ідеально)
- Вимірює схожість між справжніми та передбаченими кластерами
- Скоригований на випадкову відповідність
- **Інтерпретація**:
  - 0.9-1.0: Відмінно ✅✅
  - 0.7-0.9: Добре ✅
  - 0.5-0.7: Середньо ⚠️
  - < 0.5: Погано ❌

**5. Normalized Mutual Information (NMI)** (діапазон: [0, 1])
- Вимірює взаємну інформацію між справжніми та передбаченими кластерами
- Нормалізована версія
- **Інтерпретація**: аналогічна до ARI

### Додаткові метрики:

**Розмірність Features**
- Кількість чисел у feature vector
- Впливає на швидкість та якість

**Час екстракції**
- Скільки часу потрібно для витягування ознак
- Важливо для real-time додатків

## Структура проєкту

```
Deep-learning/01-Feature-extraction/
│
├── main.py                                    # Головний скрипт
├── requirements.txt                           # Залежності
├── README.md                                  # Документація
│
└── results/                                   # Згенеровані файли
    ├── dataset_samples.png                    # Приклади датасетів
    │
    ├── cnn_filters_conv1.png                  # Фільтри CNN
    ├── cnn_activations_progression.png        # Активації шарів
    ├── cnn_feature_maps_conv1.png            # Детальні feature maps
    │
    ├── pca_projections_MNIST_легко.png       # PCA: MNIST
    ├── pca_projections_Fashion-MNIST_важко.png  # PCA: Fashion-MNIST
    ├── pca_projections_CIFAR-10_колірні.png  # PCA: CIFAR-10
    │
    ├── tsne_projections_MNIST_легко.png      # t-SNE: MNIST
    ├── tsne_projections_Fashion-MNIST_важко.png # t-SNE: Fashion-MNIST
    ├── tsne_projections_CIFAR-10_колірні.png # t-SNE: CIFAR-10
    │
    ├── extractors_comparison.png              # Порівняння екстракторів
    │
    ├── benchmark_MNIST_легко.csv             # Метрики: MNIST
    ├── benchmark_Fashion-MNIST_важко.csv     # Метрики: Fashion-MNIST
    └── benchmark_CIFAR-10_колірні.csv        # Метрики: CIFAR-10
```

## Встановлення

### Вимоги:
- Python 3.8+
- pip
- GPU (опціонально, але рекомендується)

### Кроки встановлення:

```bash
# Перейти до директорії проєкту
cd Deep-learning/01-Feature-extraction

# (Опціонально) Створення віртуального середовища
python -m venv venv

# Активація віртуального середовища
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Встановлення залежностей
pip install -r requirements.txt
```

## Запуск

```bash
python main.py
```

Скрипт автоматично:
1. 📥 Завантажить MNIST, Fashion-MNIST, CIFAR-10
2. 🎨 Візуалізує приклади з датасетів
3. 🧠 Створить та навчить 5 екстракторів
4. 🔍 Візуалізує внутрішні представлення CNN
5. 📊 Обчислить PCA та t-SNE проекції
6. 📈 Порівняє якість кластеризації
7. 💾 Збереже всі графіки та метрики

**Очікуваний час виконання:** 
- CPU: ~15-20 хвилин
- GPU: ~5-7 хвилин

## Результати та інтерпретація

### Очікувані результати:

#### 1. MNIST (легко) ✅

**Характеристики:**
- Високий Silhouette Score (> 0.6)
- Високий ARI (> 0.8)
- Чіткі кластери на PCA/t-SNE

**Найкращі екстрактори:**
1. ResNet50 - найвища якість
2. Custom CNN - найкращий баланс
3. VGG16 - стабільні результати

**Інсайти:**
- Класи добре розділені навіть у PCA (лінійно)
- Усі екстрактори показують гарні результати
- t-SNE показує чіткі 10 кластерів

#### 2. Fashion-MNIST (важко) ⚠️

**Характеристики:**
- Нижчий Silhouette Score (0.3-0.5)
- Середній ARI (0.4-0.6)
- Перекриття класів на візуалізаціях

**Найкращі екстрактори:**
1. ResNet50 - глибокі features допомагають
2. VGG16 - стабільні результати
3. Custom CNN - добрий, але слабший

**Інсайти:**
- Класи перекриваються (напр. пальто vs. светр)
- PCA не справляється (лінійна)
- t-SNE показує краще, але все одно є перекриття
- Pretrained моделі краще за custom

#### 3. CIFAR-10 (середньо) 🎨

**Характеристики:**
- Середній Silhouette Score (0.4-0.6)
- Середній ARI (0.5-0.7)
- Деякі класи добре розділені, деякі ні

**Найкращі екстрактори:**
1. ResNet50 - найкраща якість
2. VGG16 - дуже близько до ResNet
3. MobileNetV2 - швидкість + якість

**Інсайти:**
- Колірна інформація важлива
- Автомобілі vs. вантажівки - складно
- Претреnovані моделі значно кращі
- Autoencoder слабкий без supervision

### Порівняльна таблиця (приклад):

| Extractor | Dataset | Silhouette | ARI | NMI | Час (s) |
|-----------|---------|-----------|-----|-----|---------|
| **ResNet50** | MNIST | **0.68** | **0.85** | **0.86** | 3.2 |
| **VGG16** | MNIST | 0.65 | 0.82 | 0.83 | 4.1 |
| **Custom CNN** | MNIST | 0.62 | 0.79 | 0.80 | **1.5** |
| **MobileNetV2** | MNIST | 0.60 | 0.77 | 0.78 | **1.2** |
| **Autoencoder** | MNIST | 0.55 | 0.72 | 0.73 | 1.8 |
| | | | | | |
| **ResNet50** | Fashion | **0.52** | **0.58** | **0.60** | 3.5 |
| **VGG16** | Fashion | 0.49 | 0.55 | 0.57 | 4.3 |
| **Custom CNN** | Fashion | 0.42 | 0.48 | 0.50 | **1.6** |
| **MobileNetV2** | Fashion | 0.45 | 0.51 | 0.53 | **1.3** |
| **Autoencoder** | Fashion | 0.35 | 0.40 | 0.42 | 1.9 |

### Ключові висновки:

**🏆 Якість:**
1. **ResNet50** - найкраща якість features на всіх датасетах
2. **VGG16** - близько до ResNet, але повільніший
3. **Custom CNN** - гарний баланс якості та швидкості
4. **MobileNetV2** - найшвидший при прийнятній якості
5. **Autoencoder** - найслабший (unsupervised)

**⚡ Швидкість:**
1. **MobileNetV2** - найшвидший
2. **Custom CNN** - швидкий і ефективний
3. **Autoencoder** - середній
4. **ResNet50** - повільний
5. **VGG16** - найповільніший

**💡 Рекомендації:**

| Сценарій | Рекомендація |
|----------|-------------|
| Максимальна якість | **ResNet50** |
| Обмежені ресурси | **Custom CNN** або **MobileNetV2** |
| Real-time інференція | **MobileNetV2** |
| Немає міток | **Autoencoder** |
| Баланс якості/швидкості | **Custom CNN** |

## Візуалізація: що шукати

### PCA проекції:

✅ **Добре розділені класи:**
- Окремі кластери
- Мало перекриття
- Високий explained variance

❌ **Погано розділені класи:**
- Один великий кластер
- Багато перекриття
- Низький explained variance

### t-SNE проекції:

✅ **Добрі features:**
- Чіткі "острівці" кластерів
- Простір між кластерами
- Компактні групи

❌ **Погані features:**
- Розмиті границі
- Кластери перекриваються
- Хаотичне розташування

### CNN візуалізації:

**Фільтри conv1:**
- Мають виглядати як edge/corner детектори
- Різноманітні паттерни
- Не випадковий шум

**Активації:**
- Перші шари: прості ознаки (краї)
- Глибші шари: складні ознаки (об'єкти)
- Прогресивне абстрагування

## Поширені питання

**Q: Чому pretrained моделі працюють краще на MNIST, якщо вони навчені на ImageNet?**  
A: Pretrained моделі вже вивчили універсальні візуальні ознаки (краї, текстури, форми), які корисні для будь-яких зображень. Transfer learning працює навіть між різними доменами.

**Q: Чому Autoencoder показує гірші результати?**  
A: Autoencoder навчається без міток класів (unsupervised), тому його latent space не оптимізований для розділення класів. Він просто вчиться стискати інформацію для реконструкції.

**Q: Яка різниця між PCA та t-SNE?**  
A: PCA - лінійне перетворення, швидке, детерміноване. t-SNE - нелінійне, повільне, краще для візуалізації складних структур. Використовуйте PCA спочатку, t-SNE для детального аналізу.

**Q: Скільки epochs потрібно для навчання Custom CNN?**  
A: Для MNIST/Fashion-MNIST достатньо 5-10 epochs. Для CIFAR-10 може знадобитися 20-50. Залежить від складності даних.

**Q: Чому MobileNetV2 швидший за ResNet50?**  
A: MobileNetV2 використовує depthwise separable convolutions, які значно ефективніші за стандартні згортки. Також має менше параметрів (3.5M vs 25.6M).

**Q: Як інтерпретувати CNN фільтри?**  
A: Перші шари виявляють прості ознаки (горизонтальні/вертикальні лінії, кути). Глибші шари - складні паттерни. Якщо фільтри виглядають як шум - щось не так з навчанням.

## Подальші покращення

Можливі розширення проєкту:

- 🎯 **Більше датасетів**: STL-10, ImageNet subset, custom datasets
- 🧠 **Більше architectures**: EfficientNet, Vision Transformer, DenseNet
- 📊 **Більше метрик**: UMAP замість t-SNE, Triplet loss для метричного навчання
- 🎨 **Градієнти та Saliency Maps**: Grad-CAM, Integrated Gradients
- 🔍 **Feature importance**: SHAP values для features
- ⚡ **Оптимізація**: TensorRT, ONNX, quantization
- 📈 **Supervised Contrastive Learning**: навчання кращих features
- 🎬 **Інтерактивні візуалізації**: Plotly, TensorBoard embeddings

## Технічні деталі

### Custom CNN:
- **Оптимізатор**: Adam
- **Loss**: Sparse Categorical Crossentropy
- **Batch size**: 128
- **Epochs**: 5
- **Regularization**: Dropout(0.5), BatchNormalization

### Autoencoder:
- **Оптимізатор**: Adam
- **Loss**: MSE (Mean Squared Error)
- **Batch size**: 128
- **Epochs**: 10
- **Latent dim**: 128

### Pretrained моделі:
- **Weights**: ImageNet
- **Trainable**: False (заморожені)
- **Preprocessing**: Conv2D(3, 1×1) для grayscale → RGB
- **Pooling**: GlobalAveragePooling2D

### Кластеризація:
- **Алгоритм**: K-Means
- **n_clusters**: відповідає кількості класів датасету
- **n_init**: 10
- **random_state**: 42

### PCA:
- **n_components**: 2 (для візуалізації)
- **random_state**: 42

### t-SNE:
- **n_components**: 2
- **perplexity**: 30
- **random_state**: 42

## Висновки

Цей проєкт демонструє:

1. ✅ **Завантаження та підготовку** 3 датасетів різної складності
2. ✅ **Створення власних екстракторів** (CNN, Autoencoder)
3. ✅ **Використання pretrained моделей** (VGG16, ResNet50, MobileNetV2)
4. ✅ **Візуалізацію CNN internals** (фільтри, активації)
5. ✅ **PCA та t-SNE проекції** для аналізу separability
6. ✅ **Комплексний бенчмарк** з 5 метриками
7. ✅ **Порівняння швидкості** екстракції

### Ключові інсайти:

🎯 **Для легких датасетів (MNIST):**
- Навіть прості екстрактори працюють добре
- Custom CNN - оптимальний вибір
- Класи лінійно розділимі (PCA достатньо)

⚠️ **Для складних датасетів (Fashion-MNIST):**
- Pretrained моделі показують кращі результати
- Потрібні глибокі features (ResNet50)
- Нелінійні методи (t-SNE) необхідні

🎨 **Для природних зображень (CIFAR-10):**
- Transfer learning критично важливий
- ImageNet features узагальнюються добре
- MobileNetV2 - гарний баланс

📊 **Загальні висновки:**
- Глибші мережі → кращі features
- Pretrained моделі >> custom (якщо є GPU)
- Швидкість vs. якість - завжди є компроміс
- Візуалізація важлива для розуміння

## Автор

Проєкт створено для навчальних цілей в рамках курсу з глибокого навчання (Feature Extraction).

## Ліцензія

Навчальний проєкт

---

**Примітка:** Всі результати генеруються автоматично. При першому запуску відбудеться завантаження pretrained моделей (~100MB).

**🔥 Порада:** Якщо у вас є GPU, встановіть `tensorflow-gpu` для значного прискорення!


