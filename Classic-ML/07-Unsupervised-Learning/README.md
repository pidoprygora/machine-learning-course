# Бенчмаркінг алгоритмів неконтрольованого навчання

## Опис проєкту

Цей проєкт проводить комплексний бенчмаркінг алгоритмів кластеризації з **детальною візуалізацією ітерацій** K-Means та EM (Gaussian Mixture Models). Фокус на 2D даних для наочної демонстрації роботи алгоритмів.

## Особливості проєкту

✨ **Основні можливості:**
- 📊 Генерація та візуалізація 2D датасетів (3-5 класів)
- 🔄 Покрокова візуалізація ітерацій K-Means та GMM/EM
- 📈 Графіки збіжності алгоритмів
- 🎯 Порівняння 4 алгоритмів кластеризації
- 📉 Метод ліктя для визначення оптимальної кількості кластерів
- 📊 Комплексна оцінка якості кластеризації

## Датасети

Проєкт використовує згенеровані 2D датасети з різними властивостями:

### 1. Blobs (4 кластери)
- 600 точок
- 4 чітко відокремлені гаусівські кластери
- Ідеально підходить для K-Means та GMM

### 2. Blobs з різною дисперсією
- 600 точок
- 5 кластерів з різним розкидом
- Демонструє переваги GMM над K-Means

### 3. Anisotropic
- 600 точок
- 3 витягнуті кластери
- Складніша структура для кластеризації

## Алгоритми кластеризації

### 1. K-Means Clustering
**Як працює:**
- Ініціалізує k центроїдів (випадково або k-means++)
- **E-step**: призначає кожну точку до найближчого центроїду
- **M-step**: оновлює центроїди як середнє точок кластера
- Повторює до збіжності

**Переваги:**
- ✅ Швидкий та простий
- ✅ Добре працює з округлими кластерами
- ✅ Масштабується на великі датасети

**Недоліки:**
- ❌ Чутливий до ініціалізації
- ❌ Припускає кластери однакового розміру
- ❌ Важко працює з не-сферичними кластерами

### 2. Gaussian Mixture Models (EM Algorithm)
**Як працює:**
- Моделює дані як суміш гаусівських розподілів
- **E-step**: обчислює ймовірність належності точки до кожного кластера
- **M-step**: оновлює параметри гаусіанів (середнє, коваріація, ваги)
- Максимізує log-likelihood

**Переваги:**
- ✅ М'які границі кластерів (ймовірнісний підхід)
- ✅ Може моделювати кластери різної форми та розміру
- ✅ Обробляє перекриття кластерів

**Недоліки:**
- ❌ Може перенавчатися на малих датасетах
- ❌ Чутливий до ініціалізації
- ❌ Повільніший за K-Means

### 3. DBSCAN
**Як працює:**
- Density-Based Spatial Clustering
- Знаходить кластери на основі щільності точок
- Автоматично визначає кількість кластерів

**Переваги:**
- ✅ Не потребує вказувати кількість кластерів
- ✅ Знаходить кластери довільної форми
- ✅ Виявляє викиди (шумові точки)

**Недоліки:**
- ❌ Чутливий до параметрів eps та min_samples
- ❌ Погано працює з кластерами різної щільності

### 4. Hierarchical Clustering (Agglomerative)
**Як працює:**
- Початково кожна точка - окремий кластер
- Ітеративно об'єднує найближчі кластери
- Будує дендрограму (дерево кластерів)

**Переваги:**
- ✅ Не потребує вказувати кількість кластерів заздалегідь
- ✅ Дає ієрархічну структуру даних
- ✅ Різні метрики відстані

**Недоліки:**
- ❌ Повільний на великих датасетах O(n²)
- ❌ Неможливо скасувати неправильне об'єднання

## Метрики оцінки

### Внутрішні метрики (не потребують справжніх міток):

**1. Silhouette Score** (вище = краще, діапазон: [-1, 1])
- Вимірює наскільки точка схожа на свій кластер vs. інші кластери
- Значення близько 1: точка добре класифікована
- Значення близько 0: точка на границі кластерів
- Від'ємне: точка можливо в неправильному кластері

**2. Davies-Bouldin Index** (нижче = краще, діапазон: [0, ∞))
- Середнє співвідношення внутрішньокластерної та міжкластерної відстані
- Менше значення = більш компактні та віддалені кластери

**3. Calinski-Harabasz Index** (вище = краще)
- Відношення міжкластерної дисперсії до внутрішньокластерної
- Також відомий як Variance Ratio Criterion

### Зовнішні метрики (потребують справжні мітки):

**4. Adjusted Rand Index (ARI)** (діапазон: [-1, 1], 1 = ідеальна відповідність)
- Вимірює схожість між справжніми та передбаченими кластерами
- Скоригований на випадкову відповідність

**5. Normalized Mutual Information (NMI)** (діапазон: [0, 1])
- Вимірює взаємну інформацію між справжніми та передбаченими кластерами
- Нормалізована версія взаємної інформації

## Візуалізації

### Покрокові ітерації:

**K-Means:**
- 🎬 Рух центроїдів від ітерації до ітерації
- ⭐ Центроїди позначені червоними зірками
- 📉 Графік зменшення inertia (збіжність)
- Показує як алгоритм знаходить оптимальні центри

**GMM (EM):**
- 🎨 Еліпси гаусівських розподілів (1σ, 2σ, 3σ)
- 🌟 Центри компонент та їх ваги
- 📈 Графік зростання log-likelihood (збіжність)
- Демонструє як змінюється форма кластерів

### Аналіз:
- 📊 Метод ліктя (Elbow Method)
- 📈 Silhouette Score vs кількість кластерів
- 🎯 Порівняння результатів всіх алгоритмів

## Структура проєкту

```
07-Unsupervised-Learning/
│
├── main.py                                # Головний скрипт
├── requirements.txt                       # Залежності
├── README.md                             # Документація
│
└── [Згенеровані файли після запуску]
    ├── original_data_distribution.png    # Вихідні дані
    ├── elbow_method.png                  # Метод ліктя
    ├── kmeans_iterations.png             # Ітерації K-Means
    ├── kmeans_convergence.png            # Збіжність K-Means
    ├── gmm_iterations.png                # Ітерації GMM
    ├── gmm_convergence.png               # Збіжність GMM
    ├── clustering_results_*.png          # Результати
    └── results_*.csv                     # Метрики у CSV
```

## Встановлення

### Вимоги:
- Python 3.8+
- pip

### Кроки встановлення:

```bash
# Перейти до директорії проєкту
cd 07-Unsupervised-Learning

# (Опціонально) Створення віртуального середовища
python -m venv venv

# Активація віртуального середовища
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Встановлення залежностей
pip install -r requirements.txt
```

## Запуск

```bash
python main.py
```

Скрипт автоматично:
1. 📊 Згенерує 3 різні датасети
2. 🎨 Візуалізує вихідні дані
3. 📉 Проведе аналіз методом ліктя
4. 🔄 Покаже ітерації K-Means та GMM
5. 🎯 Порівняє 4 алгоритми кластеризації
6. 💾 Збереже всі графіки та метрики

**Очікуваний час виконання:** ~30-60 секунд

## Результати

### Приклад виводу метрик:

```
                  Silhouette  Davies-Bouldin  Calinski-Harabasz    ARI     NMI
K-Means              0.5234          0.6789            542.12    0.8234  0.8456
GMM (EM)             0.5123          0.6912            538.45    0.8156  0.8389
DBSCAN               0.4567          0.7234            489.23    0.7456  0.7689
Hierarchical         0.5089          0.6856            535.67    0.8089  0.8312
```

### Інтерпретація:

**Silhouette Score:**
- 0.71-1.0: Сильна структура
- 0.51-0.70: Середня структура
- 0.26-0.50: Слабка структура
- < 0.25: Структура відсутня

**Коли використовувати який алгоритм:**

| Алгоритм | Коли використовувати |
|----------|---------------------|
| **K-Means** | Великі датасети, округлі кластери, відома кількість кластерів |
| **GMM** | Кластери різної форми/розміру, м'які границі, перекриття |
| **DBSCAN** | Невідома кількість кластерів, кластери довільної форми, є викиди |
| **Hierarchical** | Потрібна ієрархічна структура, малі/середні датасети |

## Ключові інсайти

### Переваги K-Means:
✅ Найшвидший алгоритм  
✅ Добре працює на добре відокремлених, округлих кластерах  
✅ Проста інтерпретація  

### Переваги GMM/EM:
✅ М'які ймовірнісні границі  
✅ Моделює кластери різної форми (еліптичні)  
✅ Може обробляти перекриття кластерів  

### Спостереження:
- 🎯 На простих датасетах (Blobs) K-Means та GMM показують схожі результати
- 📊 GMM краще справляється з кластерами різного розміру
- 🔄 Обидва алгоритми зазвичай збігаються за 5-10 ітерацій
- ⚡ K-Means швидший, але GMM більш гнучкий

## Візуалізація ітерацій

### Що показує K-Means:
1. **Ітерація 1:** Випадкова ініціалізація центроїдів
2. **Ітерації 2-3:** Швидкі зміни позицій центроїдів
3. **Ітерації 4-5:** Дрібні корекції
4. **Остання ітерація:** Збіжність досягнута

### Що показує GMM:
1. **Ітерація 1:** Ініціалізація гаусіанів
2. **Ітерації 2-3:** Швидка адаптація форми еліпсів
3. **Ітерації 4-5:** Налаштування ваг та коваріацій
4. **Остання ітерація:** Максимізація log-likelihood

## Методологія оцінки

### Train/Test Split:
- 70% тренувальні дані
- 30% тестові дані
- Стратифікація за класами

### Стандартизація:
- StandardScaler (mean=0, std=1)
- Важливо для алгоритмів, чутливих до масштабу

### Метод ліктя:
- Тестуємо k від 2 до 10
- Шукаємо "лікоть" на графіку inertia
- Підтверджуємо через Silhouette Score

## Поширені питання

**Q: Чому K-Means не завжди знаходить правильні кластери?**  
A: K-Means припускає сферичні кластери однакового розміру. Для складніших форм використовуйте GMM або DBSCAN.

**Q: Скільки кластерів вибрати?**  
A: Використовуйте метод ліктя, Silhouette Score, або доменні знання.

**Q: Що робити якщо результати нестабільні?**  
A: Запускайте алгоритм кілька разів (n_init), використовуйте k-means++ ініціалізацію.

**Q: Коли використовувати GMM замість K-Means?**  
A: Коли кластери мають різну форму, розмір, або перекриваються.

## Подальші покращення

Можливі розширення проєкту:

- 🎬 Анімація ітерацій (GIF або MP4)
- 🌐 Тестування на реальних датасетах (Iris, Wine, тощо)
- 📊 Більше алгоритмів (Spectral Clustering, OPTICS)
- 🔍 Аналіз впливу ініціалізації
- 📈 Порівняння часу виконання
- 🎨 Інтерактивні візуалізації (Plotly)

## Висновки

Цей проєкт демонструє:

1. ✅ **Генерацію 2D даних** з різними властивостями
2. ✅ **Покрокову візуалізацію** ітерацій K-Means та EM
3. ✅ **Графіки збіжності** обох алгоритмів
4. ✅ **Комплексне порівняння** 4 алгоритмів кластеризації
5. ✅ **Оцінку точності** через 5 різних метрик
6. ✅ **Метод ліктя** для визначення оптимального k

### Ключові висновки:

🎯 **K-Means:**
- Швидкий та ефективний для простих структур
- Збігається за 5-8 ітерацій
- Оптимальний для великих датасетів

🎨 **GMM (EM):**
- Більш гнучкий та потужний
- Моделює складні розподіли
- Ймовірнісна інтерпретація

📊 **Вибір алгоритму** залежить від:
- Структури даних
- Розміру датасету
- Потреби в інтерпретованості
- Доступних обчислювальних ресурсів

## Автор

Проєкт створено для навчальних цілей в рамках курсу з машинного навчання (неконтрольоване навчання).

## Ліцензія

Навчальний проєкт

---

**Примітка:** Всі графіки та результати генеруються автоматично при запуску `main.py`

