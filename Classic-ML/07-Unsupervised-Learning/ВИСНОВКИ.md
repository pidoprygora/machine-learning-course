# Висновки: Бенчмаркінг алгоритмів неконтрольованого навчання

## Зміст

1. [Огляд проєкту](#огляд-проєкту)
2. [Аналіз вхідних даних](#аналіз-вхідних-даних)
3. [Визначення оптимальної кількості кластерів](#визначення-оптимальної-кількості-кластерів)
4. [Аналіз ітерацій K-Means](#аналіз-ітерацій-k-means)
5. [Аналіз ітерацій GMM (EM)](#аналіз-ітерацій-gmm-em)
6. [Порівняння алгоритмів кластеризації](#порівняння-алгоритмів-кластеризації)
7. [Детальний аналіз по датасетам](#детальний-аналіз-по-датасетам)
8. [Загальні висновки](#загальні-висновки)
9. [Практичні рекомендації](#практичні-рекомендації)

---

## Огляд проєкту

Цей проєкт присвячений комплексному дослідженню алгоритмів неконтрольованого навчання з **акцентом на візуалізації процесу навчання**. Головна мета — продемонструвати як працюють алгоритми K-Means та Gaussian Mixture Models (EM) покроково, від ініціалізації до збіжності.

### Ключові особливості дослідження:

✅ **2D візуалізація** — всі датасети двовимірні для наочності  
✅ **Покрокова візуалізація** — показано еволюцію алгоритмів  
✅ **Множинні датасети** — 3 різні типи структур даних  
✅ **4 алгоритми** — K-Means, GMM, DBSCAN, Hierarchical  
✅ **5 метрик** — комплексна оцінка якості кластеризації  

---

## Аналіз вхідних даних

### Рисунок 1: Розподіл згенерованих датасетів

![Розподіл даних](original_data_distribution.png)

На **Рисунку 1** представлено три типи датасетів з різними характеристиками:

#### 1. **Blobs (4 кластери)** — ідеальний випадок
- 🎯 **600 точок, 4 класи**
- 📊 Чіткі гаусівські кластери з однаковою дисперсією
- ✅ Кластери добре відокремлені в просторі
- 🔵 Компактні, округлі форми
- **Ідеально підходить для:** K-Means, GMM, Hierarchical

**Характеристики:**
- Відстань між центрами кластерів: ~6-8 одиниць
- Радіус кластерів: ~0.8 одиниць
- Мінімальне перекриття між класами

#### 2. **Blobs (різна дисперсія)** — реалістичний випадок
- 🎯 **600 точок, 5 класів**
- 📊 Кластери мають різний розмір та щільність
- ⚠️ Дисперсія варіюється: 0.5, 1.0, 1.5, 0.7, 1.2
- 🔴 Деякі кластери компактні, інші — розсіяні

**Спостереження:**
- Верхній клас (червоний) — компактний, щільний
- Правий клас (бірюзовий) — розсіяний, великий радіус
- Нижній клас (синій) — середня щільність
- **Виклик:** K-Means припускає однакову дисперсію

#### 3. **Anisotropic** — складний випадок
- 🎯 **600 точок, 3 класи**
- 📊 Витягнуті, некруглі кластери
- 🔄 Застосована лінійна трансформація [[0.6, -0.6], [-0.4, 0.8]]
- 📐 Кластери мають різну орієнтацію та форму

**Особливості:**
- Кластери не є сферичними
- Еліптична форма з різним співвідношенням осей
- Частково перекриваються
- **Виклик:** Класична K-Means може давати субоптимальні результати

### Висновок по даним:

| Датасет | Складність | Краще працює |
|---------|-----------|--------------|
| Blobs (4) | ⭐ Легко | K-Means, GMM, Hierarchical |
| Blobs (різна) | ⭐⭐ Середньо | GMM (адаптується до розмірів) |
| Anisotropic | ⭐⭐⭐ Складно | GMM (моделює еліпси) |

---

## Визначення оптимальної кількості кластерів

### Рисунок 2: Метод ліктя та Silhouette Score

![Метод ліктя](elbow_method.png)

**Рисунок 2** показує два класичних методи визначення оптимальної кількості кластерів:

### Лівий графік: Метод ліктя (Inertia vs k)

**Що показує Inertia:**
- Сума квадратів відстаней від точок до їх центроїдів
- Завжди зменшується зі збільшенням k
- "Лікоть" — точка, де швидкість зменшення сповільнюється

**Аналіз графіка:**

1. **k=2:** Inertia ≈ 440
   - Дуже високе значення
   - Дані розділені на 2 великі групи
   - Явно недостатньо кластерів

2. **k=3:** Inertia ≈ 95 (падіння на 78%)
   - Різке зменшення inertia
   - Значне покращення якості

3. **k=4:** Inertia ≈ 20 (падіння на 79%)
   - **Чітко виражений "лікоть"** 📍
   - Найбільш різке покращення
   - Оптимальна точка

4. **k=5-10:** Inertia ≈ 10-15
   - Повільне зменшення
   - Додаткові кластери не дають суттєвого покращення
   - Ризик перенавчання (overfitting)

### Правий графік: Silhouette Score vs k

**Що показує Silhouette Score:**
- Вимірює якість кластеризації
- Діапазон: [-1, 1], де 1 — ідеальна кластеризація
- Враховує компактність та відокремленість кластерів

**Аналіз графіка:**

1. **k=2:** Silhouette ≈ 0.57
   - Помірна якість
   - Дані не оптимально розділені

2. **k=3:** Silhouette ≈ 0.77
   - Гарна якість
   - Значне покращення

3. **k=4:** Silhouette ≈ 0.84 (МАКСИМУМ) 🏆
   - **Найкраща якість кластеризації**
   - Кластери компактні та добре відокремлені
   - Оптимальна позначена червоною лінією

4. **k=5:** Silhouette ≈ 0.71
   - Зниження якості
   - Надмірна фрагментація

5. **k≥6:** Silhouette < 0.60
   - Поганішає якість
   - Занадто багато кластерів

### Висновки з методу ліктя:

✅ **Обидва методи вказують на k=4 як оптимальне**  
✅ **Silhouette Score = 0.84** — дуже хороша кластеризація  
✅ **Різке падіння після k=4** — підтверджує вибір  
⚠️ **k=3 також прийнятне** (Silhouette=0.77) але не оптимальне  

**Практичний висновок:** Для датасету Blobs використовуємо **k=4 кластери**.

---

## Аналіз ітерацій K-Means

### Рисунок 3: Візуалізація ітерацій K-Means

![K-Means ітерації](kmeans_iterations.png)

**Рисунок 3** демонструє еволюцію алгоритму K-Means від початкової ініціалізації до збіжності.

### Детальний аналіз кожної ітерації:

#### Ітерація 1: Початкова ініціалізація (Inertia: 47.69)
**Що відбувається:**
- 🎲 Центроїди ініціалізовані методом k-means++
- ⭐ Червоні зірки показують позиції центроїдів
- 📊 Точки пофарбовані за найближчим центроїдом
- 🔴 Помітні неправильні призначення точок

**Спостереження:**
- Один центроїд потрапив між двома кластерами
- Верхній правий кластер частково призначений до неправильного центроїду
- **Inertia дуже висока (47.69)** — показник поганої кластеризації
- Візуально видно, що багато точок далеко від своїх центроїдів

**Чому k-means++?**
- Краща ініціалізація, ніж випадкова
- Вибирає центроїди, які далеко один від одного
- Зменшує ймовірність поганої збіжності

#### Ітерація 2: Перше оновлення (Inertia: 16.32)
**Що відбувається:**
- 🔄 Центроїди переміщені до центрів мас своїх кластерів
- 📉 **Різке падіння Inertia: 47.69 → 16.32 (66% зменшення)**
- ✅ Більшість точок тепер правильно класифіковані
- ➡️ Пунктирні лінії показують рух центроїдів

**Спостереження:**
- Найбільший рух — у центроїда, який був між кластерами
- Перерозподіл точок привів до нової конфігурації
- Візуальна якість значно покращилася
- Кластери стали компактнішими

**M-step (оновлення центроїдів):**
```
новий_центроїд[k] = mean(всі_точки_кластера[k])
```

#### Ітерація 3: Збіжність досягнута (Inertia: 16.32)
**Що відбувається:**
- ⏸️ **Збіжність досягнута!**
- 🔒 Центроїди не змінюють позицію
- ✅ Inertia залишається незмінною
- 🎯 Кожна точка належить до найближчого центроїду

**Критерій збіжності:**
```python
if np.allclose(old_centroids, new_centroids, rtol=1e-6):
    # Збіжність досягнута
```

**Чому так швидко?**
- Датасет має чітку структуру
- Гарна ініціалізація (k-means++)
- Кластери добре відокремлені
- Немає перекриття між класами

### Рисунок 4: Крива збіжності K-Means

![K-Means збіжність](kmeans_convergence.png)

**Рисунок 4** показує графік зменшення Inertia по ітераціях.

**Аналіз кривої:**

1. **Ітерація 1 → 2:**
   - 📉 Падіння: 47.69 → 16.32
   - ⚡ Різке зменшення на **66%**
   - Найбільше покращення

2. **Ітерація 2 → 3:**
   - 📊 Inertia = 16.32 (без змін)
   - ✅ Досягнута локальна мінімум
   - Алгоритм зупиняється

**Типова поведінка K-Means:**
- Найбільше покращення на перших ітераціях
- Експоненційна збіжність до мінімуму
- Зазвичай збігається за 5-15 ітерацій

### Ключові висновки по K-Means:

✅ **Швидка збіжність** — всього 2 ітерації  
✅ **Ефективність** — різке зменшення inertia  
✅ **Стабільність** — досягнуто глобальний мінімум  
✅ **Якість** — кластери ідеально відповідають справжнім класам  

**Переваги на цьому датасеті:**
- 🚀 Дуже швидкий алгоритм
- 💡 Проста інтерпретація (центроїди)
- 🎯 Ідеальні результати для округлих кластерів
- 📊 Низька обчислювальна складність O(n·k·i·d)

**Обмеження:**
- ⚠️ Чутливий до ініціалізації (вирішено k-means++)
- ⚠️ Припускає сферичні кластери
- ⚠️ Чутливий до викидів

---

## Аналіз ітерацій GMM (EM)

### Рисунок 5: Візуалізація ітерацій Gaussian Mixture Models

![GMM ітерації](gmm_iterations.png)

**Рисунок 5** показує еволюцію EM-алгоритму для Gaussian Mixture Models. На відміну від K-Means, GMM моделює кластери як гаусівські розподіли з еліптичною формою.

### Легенда візуалізації:

- 🌫️ **Еліпси (1σ, 2σ, 3σ)** — ізолінії щільності гаусіанів
- ⭐ **Чорні зірки** — центри (середні) гаусівських розподілів
- 🔢 **w=X.XX** — ваги компонент (сума = 1.0)
- 🎨 **Кольори** — різні компоненти суміші
- ⚪ **Сірі точки** — дані (не зафарбовані за класами)

### Що таке σ (сигма)?
- 1σ (68.3% даних) — внутрішній еліпс
- 2σ (95.4% даних) — середній еліпс (товста лінія)
- 3σ (99.7% даних) — зовнішній еліпс

### Детальний аналіз ітерацій:

#### Ітерація 1: Ініціалізація (Log-Likelihood: -111.3)

**Що відбувається:**
- 🎲 Початкова ініціалізація параметрів
- 📊 Всі компоненти мають **рівні ваги (w=0.25)**
- 🔵 Еліпси показують форму кожного гаусіана
- 📐 Коваріаційні матриці ініціалізовані

**Параметри кожної компоненти:**
```
Компонента k:
  - μ_k (mean): центр гаусіана [x, y]
  - Σ_k (covariance): 2×2 матриця форми еліпса
  - w_k (weight): вага компоненти (∑w_k = 1)
```

**Спостереження:**
- Еліпси перекриваються
- Деякі гаусіани не центровані на кластерах
- Верхній лівий гаусіан (червоний) покриває дві групи точок
- **Log-Likelihood низька** — модель погано пояснює дані

**E-step (Expectation):**
```
γ_ik = P(точка_i належить до компоненти_k | дані)
```

**M-step (Maximization):**
```
μ_k = ∑(γ_ik · x_i) / ∑γ_ik
Σ_k = обновлення коваріації
w_k = ∑γ_ik / n
```

#### Ітерації 2-5: Адаптація (Log-Likelihood залишається -111.3)

**Примітка:** На цьому датасеті GMM збігається дуже швидко (практично на першій ітерації).

**Що характерно для EM на добре відокремлених даних:**
- Швидка збіжність при чіткій структурі
- Мінімальні зміни після першої ітерації
- Стабільний log-likelihood

### Рисунок 6: Крива збіжності GMM

![GMM збіжність](gmm_convergence.png)

**Рисунок 6** показує графік зміни Log-Likelihood по ітераціях.

**Аналіз кривої:**

📊 **Log-Likelihood ≈ -111.3 (стабільний)**

**Що це означає:**
- ✅ Швидка збіжність (практично миттєва)
- ✅ Дані добре описуються сумішшю гаусіанів
- ✅ Оптимальні параметри знайдені швидко

**Порівняння з K-Means:**
```
K-Means оптимізує: мінімізує Inertia (сума квадратів відстаней)
GMM оптимізує: максимізує Log-Likelihood (ймовірність даних)
```

**Чому Log-Likelihood від'ємна?**
- Це логарифм добутку малих ймовірностей
- ln(0.001) = -6.9
- Завжди від'ємна для неперервних даних
- **Вище = краще** (менш від'ємне)

### Ключові переваги GMM над K-Means:

#### 1. **М'які границі кластерів**
```
K-Means: точка належить до одного кластера (hard assignment)
GMM: точка має ймовірність належності до кожного кластера (soft assignment)
```

**Приклад:**
```python
# GMM дає ймовірності:
точка_X: [0.85 для кластера_1, 0.10 для кластера_2, 0.05 для кластера_3]
```

#### 2. **Моделювання форми кластерів**
- K-Means: лише сферичні кластери
- GMM: еліптичні кластери з різною орієнтацією
- GMM може моделювати кластери різного розміру

#### 3. **Ймовірнісна інтерпретація**
```
P(точка | модель) = ∑ w_k · N(точка | μ_k, Σ_k)
```
де N — гаусівський розподіл

#### 4. **Обробка перекриття**
- K-Means: жорстке розділення
- GMM: враховує невизначеність при перекритті

### Висновки по GMM:

✅ **Гнучкість** — моделює еліптичні форми  
✅ **Ймовірнісний підхід** — м'які границі  
✅ **Адаптивність** — різні розміри кластерів  
✅ **Швидка збіжність** — на структурованих даних  

**Коли використовувати GMM:**
- 🔄 Кластери мають різну форму/розмір
- 🤝 Кластери перекриваються
- 📊 Потрібна ймовірнісна інтерпретація
- 🎯 Важлива невизначеність класифікації

**Недоліки:**
- ⚠️ Повільніший за K-Means
- ⚠️ Більше параметрів (може перенавчатися)
- ⚠️ Чутливий до вибору типу коваріації

---

## Порівняння алгоритмів кластеризації

У цьому розділі порівнюємо 4 алгоритми на трьох різних датасетах.

### Тестовані алгоритми:

1. **K-Means** — класичний метод на основі центроїдів
2. **GMM (EM)** — ймовірнісна модель гаусівських сумішей
3. **DBSCAN** — density-based, не потребує задавати k
4. **Hierarchical** — ієрархічна агломеративна кластеризація

---

## Детальний аналіз по датасетам

### Датасет 1: Blobs (4 кластери) — ідеальний випадок

#### Рисунок 7: Результати кластеризації на Blobs

![Blobs результати](clustering_results_Blobs_4_кластери.png)

**Рисунок 7** показує результати всіх 4 алгоритмів на датасеті з чіткими кластерами.

#### Візуальний аналіз:

**K-Means:**
- ✅ Ідеальне розділення
- ✅ Кожен кластер чітко виділений
- ✅ Границі прямолінійні (Voronoi діаграма)
- 🎯 4 кластери знайдені правильно

**GMM (EM):**
- ✅ Також ідеальне розділення
- ✅ Результат візуально ідентичний K-Means
- 🎨 Однакові кольори для однакових кластерів
- 📊 4 компоненти

**DBSCAN:**
- ⚠️ Знайшов лише **3 кластери**
- 🔍 Об'єднав два близькі кластери
- ✖️ Можливі викиди (чорні хрестики)
- 📉 Чутливий до параметрів eps та min_samples

**Hierarchical:**
- ✅ Ідеальне розділення
- ✅ Результат ідентичний K-Means та GMM
- 🌳 4 кластери визначені правильно
- ✅ Добре працює на сферичних кластерах

#### Таблиця метрик:

| Метрика | K-Means | GMM | DBSCAN | Hierarchical |
|---------|---------|-----|--------|--------------|
| **Silhouette Score** | 0.8367 | 0.8367 | 0.7633 | 0.8367 |
| **Davies-Bouldin** | 0.2296 | 0.2296 | 0.3486 | 0.2296 |
| **Calinski-Harabasz** | 10067 | 10067 | 2376 | 10067 |
| **ARI** | 1.0 | 1.0 | 0.7133 | 1.0 |
| **NMI** | 1.0 | 1.0 | 0.8571 | 1.0 |
| **Кластери** | 4 | 4 | 3 | 4 |

#### Інтерпретація метрик:

**Silhouette Score (вище = краще):**
- 📊 K-Means, GMM, Hierarchical: **0.8367** — чудова якість
- 📉 DBSCAN: **0.7633** — гарна, але нижча (через 3 кластери)
- 🎯 Значення > 0.7 вказує на сильну структуру

**Davies-Bouldin Index (нижче = краще):**
- ✅ K-Means, GMM, Hierarchical: **0.2296** — чудово
- ⚠️ DBSCAN: **0.3486** — гірше (менш компактні кластери)
- 📏 Вимірює співвідношення внутрішньої та міжкластерної відстані

**Calinski-Harabasz Index (вище = краще):**
- 🏆 K-Means, GMM, Hierarchical: **10067** — дуже високе
- 📉 DBSCAN: **2376** — значно нижче
- 📊 Відношення міжкластерної до внутрішньокластерної дисперсії

**ARI (Adjusted Rand Index):**
- 🎯 K-Means, GMM, Hierarchical: **1.0** — ідеальна відповідність!
- ⚠️ DBSCAN: **0.7133** — гарна, але не ідеальна
- 💯 1.0 означає повну відповідність справжнім міткам

**NMI (Normalized Mutual Information):**
- 🎯 K-Means, GMM, Hierarchical: **1.0** — ідеальна відповідність!
- ⚠️ DBSCAN: **0.8571** — дуже гарна
- 📊 Вимірює взаємну інформацію між передбаченими та справжніми мітками

#### Висновки для Blobs (4 кластери):

🥇 **ПЕРЕМОЖЦІ: K-Means, GMM, Hierarchical** (ідентичні результати)
- Всі три показали **ідеальну** кластеризацію (ARI = NMI = 1.0)
- Silhouette Score = 0.84 (чудово)
- Швидкість: K-Means > Hierarchical > GMM

🥉 **DBSCAN** — програв через:
- Неправильний вибір параметрів (eps, min_samples)
- Об'єднав два близькі кластери
- На простих сферичних кластерах не має переваг

**Рекомендація:** На таких даних використовувати **K-Means** (найшвидший).

---

### Датасет 2: Blobs (різна дисперсія) — реалістичний випадок

#### Рисунок 8: Результати на кластерах з різною дисперсією

![Blobs різна дисперсія](clustering_results_Blobs_різна_дисперсія.png)

**Рисунок 8** демонструє як алгоритми справляються з кластерами різного розміру.

#### Візуальний аналіз:

**K-Means:**
- ✅ Знайшов всі 5 кластерів
- ⚠️ Великий розсіяний кластер розділений правильно
- 📊 Компактні кластери класифіковані ідеально
- 🎯 Добре працює навіть з різною дисперсією

**GMM (EM):**
- ✅✅ Знайшов всі 5 кластерів
- 🌟 **Найкращий візуальний результат**
- 🎨 Адаптувався до розміру кластерів
- 📐 Еліптична форма дозволяє краще покриття

**DBSCAN:**
- ❌ Знайшов лише **3 кластери**
- ⚠️ Великі кластери поглинули малі
- ✖️ Можливо багато викидів (чорні хрестики)
- 📉 Погано працює з різною щільністю

**Hierarchical:**
- ✅ Знайшов всі 5 кластерів
- ✅ Хороші результати
- ⚠️ Трохи гірше, ніж GMM
- 🌳 Стабільний на різних розмірах

#### Таблиця метрик:

| Метрика | K-Means | GMM | DBSCAN | Hierarchical |
|---------|---------|-----|--------|--------------|
| **Silhouette Score** | 0.6890 | **0.6870** | 0.6115 | 0.6681 |
| **Davies-Bouldin** | **0.4595** | 0.4601 | 0.4382 | 0.4903 |
| **Calinski-Harabasz** | **4269** | 4220 | 1075 | 3992 |
| **ARI** | 0.9228 | **0.9337** | 0.4811 | 0.8907 |
| **NMI** | 0.9255 | **0.9365** | 0.7425 | 0.9188 |
| **Кластери** | 5 | 5 | 3 | 5 |

#### Інтерпретація:

**Silhouette Score:**
- 📊 Всі значення в діапазоні 0.61-0.69 (середня-гарна структура)
- 🥇 K-Means: 0.6890 (найвищий)
- 🥈 GMM: 0.6870 (близько)
- 🥉 Hierarchical: 0.6681
- 📉 DBSCAN: 0.6115 (найнижчий)

**ARI та NMI (найважливіші для якості):**
- 🥇 **GMM: ARI=0.9337, NMI=0.9365** — НАЙКРАЩИЙ!
- 🥈 K-Means: ARI=0.9228, NMI=0.9255 — відмінно
- 🥉 Hierarchical: ARI=0.8907, NMI=0.9188 — дуже добре
- 📉 DBSCAN: ARI=0.4811, NMI=0.7425 — погано

#### Висновки для Blobs (різна дисперсія):

🥇 **ПЕРЕМОЖЕЦЬ: GMM (EM)**
- Найвища відповідність справжнім міткам (ARI=0.93)
- Адаптується до кластерів різного розміру
- Еліптична модель дає перевагу

🥈 **K-Means** — дуже близько до GMM
- Трохи нижча точність (ARI=0.92)
- Але швидший за GMM
- Хороший компроміс швидкість/якість

🥉 **Hierarchical** — добрі результати
- ARI=0.89 — стабільний результат
- Не потребує ініціалізації

❌ **DBSCAN** — програв
- Не справився з різною щільністю
- Знайшов лише 3 кластери замість 5
- Потребує ретельного налаштування параметрів

**Ключовий інсайт:** Коли кластери мають **різний розмір та дисперсію**, GMM показує кращі результати, ніж K-Means.

---

### Датасет 3: Anisotropic — складний випадок

#### Рисунок 9: Результати на витягнутих кластерах

![Anisotropic результати](clustering_results_Anisotropic.png)

**Рисунок 9** показує як алгоритми справляються з некруглими, еліптичними кластерами.

#### Візуальний аналіз:

**K-Means:**
- ✅ Знайшов 3 кластери
- ⚠️ Границі прямолінійні (не підходять для еліпсів)
- 📊 Все ж таки непогана класифікація
- 🔶 Деякі точки на границях класифіковані неоптимально

**GMM (EM):**
- ✅ Знайшов 3 кластери
- ✅✅ Краще адаптувався до еліптичної форми
- 📐 Еліптична модель природна для таких даних
- 🎯 Більш плавні границі

**DBSCAN:**
- ❌ Катастрофа! Знайшов лише **2 кластери**
- ✖️✖️ Багато викидів (чорні хрестики)
- 📉 Не може впоратися з різною щільністю
- ⚠️ Параметри eps та min_samples неоптимальні

**Hierarchical:**
- ⚠️ Знайшов 3 кластери
- ❌ **Найгірший результат серед "нормальних" алгоритмів**
- 🔴 Неправильно розділив витягнуті кластери
- 📉 Euclidean distance не підходить для таких форм

#### Таблиця метрик:

| Метрика | K-Means | GMM | DBSCAN | Hierarchical |
|---------|---------|-----|--------|--------------|
| **Silhouette Score** | 0.4717 | **0.4719** | NaN | 0.4513 |
| **Davies-Bouldin** | 0.6466 | **0.6318** | NaN | 0.6595 |
| **Calinski-Harabasz** | **1105** | 1074 | NaN | 953 |
| **ARI** | **0.9386** | 0.8182 | 0.0039 | 0.3684 |
| **NMI** | **0.9122** | 0.8091 | 0.0094 | 0.5160 |
| **Кластери** | 3 | 3 | 2 | 3 |

#### Інтерпретація (складний випадок):

**Silhouette Score:**
- 📊 Всі значення ~0.45-0.47 (слабка структура)
- Це **нормально** для витягнутих кластерів
- 🥇 GMM: 0.4719 (найвищий, але різниця мінімальна)
- 📉 Hierarchical: 0.4513 (найнижчий)

**ARI та NMI (сюрприз!):**
- 🥇 **K-Means: ARI=0.9386, NMI=0.9122** — НЕОЧІКУВАНО НАЙКРАЩИЙ!
- 🥈 GMM: ARI=0.8182, NMI=0.8091 — добре, але гірше
- ❌ Hierarchical: ARI=0.3684, NMI=0.5160 — погано
- ❌❌ DBSCAN: ARI≈0, NMI≈0 — повний провал

#### Парадокс результатів:

**Чому K-Means переміг GMM на анізотропних даних?**

🤔 **Можливі причин и:**

1. **Гарна сепарація:**
   - Навіть витягнуті кластери добре відокремлені
   - K-Means може розділити їх лінійними границями
   - Перекриття мінімальне

2. **Проблема GMM:**
   - GMM може перенавчитися на малих датасетах
   - Еліптична модель має більше параметрів
   - Можлива субоптимальна збіжність EM

3. **Справжні мітки:**
   - Можливо, справжні мітки самі отримані K-Means
   - Це пояснює високий ARI для K-Means

4. **Розмір датасету:**
   - 600 точок може бути недостатньо для GMM
   - K-Means простіший і стабільніший

**Висновок:** На цьому конкретному датасеті K-Means несподівано показав кращі результати, незважаючи на теоретичні переваги GMM.

#### Висновки для Anisotropic:

🥇 **ПЕРЕМОЖЕЦЬ: K-Means** (несподівано!)
- ARI=0.94 — найвища відповідність
- Простіший і стабільніший
- Добре розділив навіть витягнуті кластери

🥈 **GMM** — добре, але не найкраще
- ARI=0.82 — гарний результат
- Теоретично повинен бути кращим
- Можливо субоптимальна збіжність

❌ **Hierarchical** — погано
- ARI=0.37 — неприйнятно
- Euclidean відстань не підходить

❌❌ **DBSCAN** — катастрофа
- Не знайшов правильну структуру
- Багато викидів
- Потребує складного налаштування

---

## Загальні висновки

### Підсумкова таблиця переможців:

| Датасет | 🥇 Місце | 🥈 Місце | 🥉 Місце | Критерій |
|---------|---------|---------|---------|----------|
| **Blobs (4)** | K-Means, GMM, Hier (ідентичні) | - | DBSCAN | ARI=1.0 |
| **Blobs (різна)** | GMM | K-Means | Hierarchical | ARI=0.93 |
| **Anisotropic** | K-Means | GMM | Hierarchical | ARI=0.94 |

### Загальний рейтинг алгоритмів:

#### 🥇 **K-Means** — Universal Champion

**Перемоги:** 2/3 датасети (+ 1 нічия)

**Сильні сторони:**
- ⚡ Найшвидший алгоритм O(n·k·i·d)
- 🎯 Стабільні результати
- 💡 Проста інтерпретація
- 🚀 Масштабується на великі дані
- ✅ Добре працює на більшості реальних задач

**Слабкі сторони:**
- ⚠️ Припускає сферичні кластери
- ⚠️ Чутливий до викидів
- ⚠️ Потрібно задавати k
- ⚠️ Локальні мінімуми (вирішено k-means++)

**Коли використовувати:**
- Великі датасети (>10K точок)
- Обмежені обчислювальні ресурси
- Кластери приблизно однакового розміру
- Потрібна швидкість

**Типові застосування:**
- Сегментація клієнтів
- Стиснення зображень
- Попередня обробка даних
- Ініціалізація для інших алгоритмів

#### 🥈 **GMM (EM)** — Sophisticated Choice

**Перемоги:** 1/3 датасети (+ 1 нічия)

**Сильні сторони:**
- 📊 М'які ймовірнісні границі
- 📐 Моделює еліптичні кластери
- 🎨 Адаптується до різних розмірів
- 🔬 Теоретично обґрунтований
- 📈 Дає ймовірності належності

**Слабкі сторони:**
- 🐌 Повільніший за K-Means
- 💾 Більше параметрів (O(k·d²))
- ⚠️ Може перенавчатися
- 🎲 Чутливий до ініціалізації
- 📏 Потребує більше даних

**Коли використовувати:**
- Кластери різної форми/розміру
- Потрібна ймовірнісна інтерпретація
- Кластери перекриваються
- Є достатньо даних
- Важлива невизначеність

**Типові застосування:**
- Розпізнавання мови (Gaussian Mixture Models)
- Моделювання розподілів
- Аномалії detection
- Біоінформатика

#### 🥉 **Hierarchical Clustering** — Stable Performer

**Результати:** 0 перемог, але стабільний

**Сильні сторони:**
- 🌳 Дає ієрархічну структуру
- 📊 Не потрібно задавати k заздалегідь
- 🔍 Дендрограма допомагає вибрати k
- ✅ Детерміністичний (без випадковості)
- 🎯 Різні метрики відстані

**Слабкі сторони:**
- 🐌🐌 Дуже повільний O(n²log n) - O(n³)
- 💾 Великі витрати пам'яті O(n²)
- ⚠️ Не може скасувати неправильне об'єднання
- 📉 Не масштабується на великі дані
- 🔒 Неможливо оновлювати модель

**Коли використовувати:**
- Малі датасети (<1000 точок)
- Потрібна ієрархічна структура
- Дослідницький аналіз даних
- Невідома кількість кластерів
- Таксономія, філогенетика

**Типові застосування:**
- Біологічна таксономія
- Аналіз соціальних мереж
- Організація документів
- Ієрархія товарів

#### ❌ **DBSCAN** — Специалізований інструмент

**Результати:** 0 перемог, 3 програші

**Сильні сторони:**
- 🔍 Не потрібно задавати k
- 🌟 Знаходить кластери довільної форми
- 🎯 Виявляє викиди (outliers)
- 📊 Робастний до шуму
- 🔄 Може знайти кластери в кластерах

**Слабкі сторони:**
- ⚙️ Дуже чутливий до параметрів (eps, min_samples)
- 📉 Погано працює з різною щільністю
- 🎲 Важко вибрати параметри
- 💾 O(n log n) з правильними структурами
- ❌ Не дає центри кластерів

**Коли використовувати:**
- Кластери довільної форми
- Різна щільність (з HDBSCAN)
- Важливо знайти викиди
- Невідома кількість кластерів
- Просторові дані

**Типові застосування:**
- Аналіз географічних даних
- Виявлення аномалій
- Computer vision (сегментація)
- Дані з різною щільністю

**Чому DBSCAN програв у нашому дослідженні:**
- Параметри (eps=0.5, min_samples=5) не оптимальні
- Потрібне ретельне налаштування для кожного датасету
- На гаусівських даних немає переваг
- Складність налаштування > переваги на простих даних

---

## Практичні рекомендації

### Блок-схема вибору алгоритму:

```
Почати тут
    ↓
Чи знаєте ви кількість кластерів (k)?
    ↓
[ТАК] → Чи кластери приблизно сферичні та однакового розміру?
    ↓
    [ТАК] → Чи датасет великий (>10K)?
        ↓
        [ТАК] → ✅ K-MEANS (швидкий і точний)
        [НІ] → Чи потрібна ймовірнісна інтерпретація?
            ↓
            [ТАК] → ✅ GMM (ймовірності)
            [НІ] → ✅ K-MEANS або Hierarchical
    ↓
    [НІ] → Чи кластери різної форми/розміру?
        ↓
        [ТАК] → ✅ GMM (еліптичні кластери)
        [НІ] → Чи є перекриття?
            ↓
            [ТАК] → ✅ GMM (м'які границі)
            [НІ] → ✅ K-MEANS
↓
[НІ] → Чи важливо знайти викиди?
    ↓
    [ТАК] → Чи кластери довільної форми?
        ↓
        [ТАК] → ⚙️ DBSCAN (потребує налаштування)
        [НІ] → ✅ Hierarchical → дендрограма → вибрати k
    ↓
    [НІ] → Чи датасет малий (<1000)?
        ↓
        [ТАК] → ✅ Hierarchical (дає ієрархію)
        [НІ] → ✅ Метод ліктя → K-MEANS або GMM
```

### Таблиця швидкого вибору:

| Ситуація | Рекомендація | Альтернатива |
|----------|-------------|--------------|
| Швидкість критична | K-Means | - |
| Кластери різного розміру | GMM | K-Means |
| Невідоме k | Hierarchical | Метод ліктя + K-Means |
| Потрібні ймовірності | GMM | - |
| Еліптичні кластери | GMM | K-Means (якщо добре відокремлені) |
| Є викиди | DBSCAN | GMM (robust fitting) |
| Малий датасет | Hierarchical | GMM |
| Великий датасет | K-Means | Mini-batch K-Means |
| Перекриття кластерів | GMM | Fuzzy C-Means |
| Потрібна ієрархія | Hierarchical | - |

### Поради по налаштуванню:

#### K-Means:
```python
# Оптимальні параметри:
KMeans(
    n_clusters=k,          # Використати метод ліктя
    init='k-means++',      # ЗАВЖДИ (краща ініціалізація)
    n_init=10,            # Мінімум 10 запусків
    max_iter=300,         # Зазвичай вистачає
    random_state=42       # Для відтворюваності
)
```

**Як вибрати k:**
1. Метод ліктя (Elbow method)
2. Silhouette Score
3. Gap statistic
4. Доменні знання

#### GMM:
```python
# Оптимальні параметри:
GaussianMixture(
    n_components=k,
    covariance_type='full',  # Повна матриця (найгнучкіше)
    # Або 'diag', 'spherical', 'tied' для простіших моделей
    max_iter=100,
    n_init=10,               # Кілька ініціалізацій
    random_state=42
)
```

**Типи коваріації:**
- `full`: найгнучкіше, але O(k·d²) параметрів
- `tied`: одна матриця для всіх кластерів
- `diag`: діагональна (швидше)
- `spherical`: як K-Means

#### Hierarchical:
```python
# Оптимальні параметри:
AgglomerativeClustering(
    n_clusters=k,
    linkage='ward',        # Найкраще для більшості випадків
    # Або 'complete', 'average', 'single'
)
```

**Методи зв'язування:**
- `ward`: мінімізує дисперсію (як K-Means)
- `complete`: максимальна відстань
- `average`: середня відстань
- `single`: мінімальна відстань (схильний до ланцюгів)

#### DBSCAN:
```python
# Складні параметри:
DBSCAN(
    eps=?,                 # Радіус околу (КРИТИЧНИЙ!)
    min_samples=?,         # Мінімум точок для ядра
    metric='euclidean'
)
```

**Як вибрати eps:**
1. k-distance графік (зазвичай k=4)
2. Візуально на гістограмі відстаней
3. % даних як "core points" (~95%)

**Емпіричні правила:**
- `min_samples` ≈ 2 × dimensionality
- `eps` залежить від масштабу даних (використати StandardScaler!)

### Типові помилки та рішення:

#### ❌ Помилка 1: Забути стандартизувати дані

```python
# ПОГАНО:
kmeans.fit(X)

# ДОБРЕ:
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)
kmeans.fit(X_scaled)
```

**Чому важливо:**
- Ознаки з різними масштабами
- Алгоритми чутливі до відстаней
- Особливо для K-Means, Hierarchical, DBSCAN

#### ❌ Помилка 2: Використовувати випадкову ініціалізацію K-Means

```python
# ПОГАНО:
KMeans(n_clusters=k, init='random')

# ДОБРЕ:
KMeans(n_clusters=k, init='k-means++', n_init=10)
```

#### ❌ Помилка 3: Не перевіряти метод ліктя

```python
# ДОБРЕ:
inertias = []
silhouettes = []
for k in range(2, 11):
    kmeans = KMeans(n_clusters=k, random_state=42)
    kmeans.fit(X)
    inertias.append(kmeans.inertia_)
    silhouettes.append(silhouette_score(X, kmeans.labels_))

# Знайти "лікоть" на графіку
```

#### ❌ Помилка 4: Використовувати DBSCAN без налаштування

```python
# ПОГАНО:
DBSCAN().fit(X)  # Дефолтні параметри рідко працюють

# ДОБРЕ:
from sklearn.neighbors import NearestNeighbors

# k-distance графік
neighbors = NearestNeighbors(n_neighbors=4)
neighbors.fit(X)
distances, indices = neighbors.kneighbors(X)
distances = np.sort(distances[:, -1])
plt.plot(distances)
# Знайти "коліно" → це eps
```

#### ❌ Помилка 5: Ігнорувати тип коваріації в GMM

```python
# На великих датасетах з багатьма ознаками:
# ПОГАНО:
GaussianMixture(n_components=k, covariance_type='full')  # Повільно

# ДОБРЕ:
GaussianMixture(n_components=k, covariance_type='diag')  # Швидше
```

### Комбіновані підходи:

#### 1. K-Means → GMM (ініціалізація)
```python
# Швидка ініціалізація GMM через K-Means
kmeans = KMeans(n_clusters=k).fit(X)
gmm = GaussianMixture(n_components=k, means_init=kmeans.cluster_centers_)
gmm.fit(X)
```

#### 2. Hierarchical → визначити k → K-Means
```python
# Hierarchical для вибору k, потім K-Means для швидкості
hier = AgglomerativeClustering(n_clusters=None, distance_threshold=threshold)
hier.fit(X)
k = len(np.unique(hier.labels_))

kmeans = KMeans(n_clusters=k).fit(X)
```

#### 3. DBSCAN → видалити викиди → K-Means
```python
# Видалити викиди, потім кластеризувати чисті дані
dbscan = DBSCAN(eps=eps, min_samples=5).fit(X)
core_samples = X[dbscan.labels_ != -1]

kmeans = KMeans(n_clusters=k).fit(core_samples)
```

---

## Фінальні висновки

### 🎯 Головні інсайти дослідження:

1. **K-Means — король швидкості та простоти**
   - Найкращий вибір для 80% реальних задач
   - Ідеальний баланс швидкість/якість
   - Працює чудово на структурованих даних

2. **GMM — потужний інструмент для складних випадків**
   - Перемагає на кластерах різного розміру
   - Ймовірнісна інтерпретація — велика перевага
   - Потребує більше даних та часу

3. **Hierarchical — стабільний вибір для малих даних**
   - Дає додаткову інформацію (дендрограма)
   - Не потребує задавати k
   - Не масштабується

4. **DBSCAN — потребує експертизи**
   - Дуже чутливий до параметрів
   - Не показав переваг на гаусівських даних
   - Корисний для специфічних випадків

### 📊 Рекомендації за категоріями:

**Для початківців:** K-Means (простий, швидкий, зрозумілий)

**Для дослідників:** GMM (гнучкість, ймовірності, теоретична основа)

**Для малих даних:** Hierarchical (стабільність, ієрархія, візуалізація)

**Для експертів:** DBSCAN + ручне налаштування (складні форми, викиди)

### 🚀 Подальші напрямки:

1. **Тестування на реальних датасетах:**
   - Iris, Wine, Digits
   - Зображення (MNIST)
   - Текстові дані (TF-IDF + кластеризація)

2. **Розширені алгоритми:**
   - Spectral Clustering
   - Mean Shift
   - HDBSCAN (покращений DBSCAN)
   - OPTICS

3. **Оцінка стабільності:**
   - Bootstrap sampling
   - Consensus clustering
   - Аналіз чутливості

4. **Візуалізація:**
   - t-SNE проєкції
   - UMAP для великих датасетів
   - Інтерактивні графіки (Plotly)

### ✅ Виконано в проєкті:

- ✅ Генерація 2D даних (3 датасети, 3-5 класів)
- ✅ Візуалізація розподілу даних
- ✅ **Покрокова візуалізація ітерацій K-Means та EM**
- ✅ Оцінка точності кластеризації (5 метрик)
- ✅ Порівняння 4 алгоритмів
- ✅ Графіки збіжності
- ✅ Метод ліктя
- ✅ Детальні висновки з посиланнями на рисунки

### 📈 Кількісні результати:

| Алгоритм | Середній ARI | Середній Silhouette | Швидкість |
|----------|--------------|---------------------|-----------|
| K-Means | 0.954 | 0.666 | ⚡⚡⚡ |
| GMM | 0.894 | 0.665 | ⚡⚡ |
| Hierarchical | 0.753 | 0.652 | ⚡ |
| DBSCAN | 0.399 | 0.688* | ⚡⚡ |

*DBSCAN має високий Silhouette на одному датасеті, але низькі ARI через неправильну кількість кластерів

### 🏆 Підсумковий рейтинг:

1. 🥇 **K-Means** — 9.5/10
   - Швидкість: 10/10
   - Точність: 9.5/10
   - Простота: 10/10
   - Гнучкість: 8/10

2. 🥈 **GMM (EM)** — 8.5/10
   - Швидкість: 7/10
   - Точність: 9/10
   - Простота: 7/10
   - Гнучкість: 10/10

3. 🥉 **Hierarchical** — 7.5/10
   - Швидкість: 5/10
   - Точність: 8/10
   - Простота: 9/10
   - Гнучкість: 8/10

4. ❌ **DBSCAN** — 5/10
   - Швидкість: 8/10
   - Точність: 4/10
   - Простота: 3/10
   - Гнучкість: 7/10

---

## Посилання на всі графіки

### Вхідні дані:
- **Рисунок 1:** [original_data_distribution.png](original_data_distribution.png) — Розподіл трьох датасетів

### Визначення k:
- **Рисунок 2:** [elbow_method.png](elbow_method.png) — Метод ліктя та Silhouette Score

### Ітерації алгоритмів:
- **Рисунок 3:** [kmeans_iterations.png](kmeans_iterations.png) — Візуалізація ітерацій K-Means
- **Рисунок 4:** [kmeans_convergence.png](kmeans_convergence.png) — Крива збіжності K-Means
- **Рисунок 5:** [gmm_iterations.png](gmm_iterations.png) — Візуалізація ітерацій GMM (EM)
- **Рисунок 6:** [gmm_convergence.png](gmm_convergence.png) — Крива збіжності GMM

### Результати кластеризації:
- **Рисунок 7:** [clustering_results_Blobs_4_кластери.png](clustering_results_Blobs_4_кластери.png) — Результати на Blobs (4 кластери)
- **Рисунок 8:** [clustering_results_Blobs_різна_дисперсія.png](clustering_results_Blobs_різна_дисперсія.png) — Результати на Blobs (різна дисперсія)
- **Рисунок 9:** [clustering_results_Anisotropic.png](clustering_results_Anisotropic.png) — Результати на Anisotropic

### Таблиці метрик (CSV):
- [results_Blobs_4_кластери.csv](results_Blobs_4_кластери.csv)
- [results_Blobs_різна_дисперсія.csv](results_Blobs_різна_дисперсія.csv)
- [results_Anisotropic.csv](results_Anisotropic.csv)

---

**Дата аналізу:** Грудень 2024  
**Автор:** Проєкт з курсу Machine Learning (Unsupervised Learning)  
**Інструменти:** Python, scikit-learn, matplotlib, seaborn, pandas, numpy

---

*Цей документ містить повний аналіз експериментів з алгоритмами неконтрольованого навчання з акцентом на візуалізації процесу навчання та детальному порівнянні результатів.*

