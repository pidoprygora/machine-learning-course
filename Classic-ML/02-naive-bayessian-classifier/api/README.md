# 🔔 USD/UAH Volatility Prediction API (Naive Bayes)

## ⚠️ ВАЖЛИВЕ ПОПЕРЕДЖЕННЯ

**Ця модель НЕ рекомендується для практичного використання!**

### Чому Naive Bayes провалився:
- **AUC = 0.53** (ледве краще за випадкове гадання)
- **Precision = 0.16** (84% прогнозів "великий рух" — помилкові!)
- **Balanced Accuracy = 0.57** (слабка модель)

### Причини:
1. Всі ознаки волатильності сильно корельовані (`ATR`, `rolling_std`, `abs_return`)
2. Naive Bayes припускає незалежність ознак — це критично порушено
3. Фінансові дохідності мають "товсті хвости", не описані нормальним розподілом

---

## 📊 Постановка задачі

**До змін:** Прогнозування напрямку руху ціни (вгору/вниз)  
**Після змін:** Класифікація днів з великою амплітудою/волатильністю

### Нова логіка:
- Обчислюємо 75-й перцентиль абсолютної дохідності на **train** (без витоку даних)
- Якщо `|ret_tomorrow| >= threshold` → `y_vol = 1` (великий рух)
- Інакше → `y_vol = 0` (звичайний день)

---

## 🔧 Зміни в API

### 1. Нові ознаки (11 замість 4):
```python
[
    "return_1d", "range_pct", "gap_open",      # базові
    "return_2d", "return_3d",                  # лагові дохідності
    "rolling_std_5", "rolling_std_10",         # ковзна волатильність
    "atr_5", "atr_10",                         # Average True Range
    "abs_gap", "abs_return"                    # абсолютні значення
]
```

### 2. Власна реалізація `InHouseGNB`:
- Підтримка `sample_weight` для балансування класів
- Boosting для меншинного класу (множник 2.5x)
- Регуляризація дисперсій (`reg_param=1e-3`)

### 3. Оптимізація порогу:
- Автоматичний пошук оптимального порога (maximize F1-score)
- Зазвичай знаходить `threshold ~ 0.2` (низький поріг для високого recall)

### 4. Змінені response:
```json
{
  "prediction": "large_move" або "normal_day",
  "probability_large_move": 0.65,
  "confidence": "medium",
  "warning": "⚠️ Модель має низьку точність..."
}
```

---

## 🚀 Як запустити

### 1. Встановити залежності:
```bash
cd api
pip install -r requirements.txt
```

### 2. Запустити сервер:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8001
```

### 3. Відкрити документацію:
```
http://localhost:8001/docs
```

---

## 📡 Ендпоінти

### `GET /health`
Перевірка стану API
```bash
curl http://localhost:8001/health
```

### `POST /upload-csv`
Завантажити CSV та перенавчити модель
```bash
curl -X POST "http://localhost:8001/upload-csv" \
  -F "file=@USD_UAH Historical Data.csv"
```

### `POST /predict`
Передбачення волатильності
```bash
curl -X POST "http://localhost:8001/predict" \
  -H "Content-Type: application/json" \
  -d '{
    "price": 41.5,
    "open_price": 41.3,
    "high_price": 41.7,
    "low_price": 41.2
  }'
```

**Приклад відповіді:**
```json
{
  "prediction": "large_move",
  "probability_large_move": 0.68,
  "confidence": "medium",
  "features_used": {
    "price": 41.5,
    "open": 41.3,
    "high": 41.7,
    "low": 41.2,
    "calculated_range": 0.5
  },
  "warning": "⚠️ Без історичних даних точність знижена! Модель має AUC~0.53..."
}
```

### `GET /model-info`
Детальна інформація про модель
```bash
curl http://localhost:8001/model-info
```

**Відповідь включає:**
- Типи використаних ознак
- Очікувані метрики (AUC, precision, recall)
- Обмеження моделі
- Рекомендації щодо альтернатив

---

## ⚖️ Порівняння з LDA

| Метрика | Naive Bayes | LDA (03-gaussian) |
|---------|-------------|-------------------|
| Balanced Accuracy | 0.57 | **0.62** ✅ |
| F1-score | 0.27 | **0.35** ✅ |
| Precision | 0.16 | **0.36** ✅ (в 2 рази краще!) |
| Recall | 0.83 | 0.33 |
| AUC | 0.53 | ~0.47 |

**Висновок:** LDA краща для цієї задачі, бо моделює кореляції між ознаками.

---

## 💡 Коли Naive Bayes ПРАЦЮЄ для валют

### ✅ Добре:
1. **Sentiment analysis новин** (Multinomial NB)
   ```python
   news = "НБУ підвищив облікову ставку"
   → передбачити: ослаблення/зміцнення гривні
   ```

2. **Класифікація режимів ринку** (дискретні ознаки)
   ```python
   [trend_strength, volatility_level, breakout] 
   → тренд/флет/волатильний
   ```

3. **Реакція на економічні події** (категоріальні)
   ```python
   [event_type, surprise, importance] 
   → ціна вгору/вниз через годину
   ```

### ❌ Погано:
- Статистика цін з корельованими індикаторами (наш випадок!)
- Точне прогнозування значення ціни
- Складні нелінійні залежності

---

## 🔬 Технічні деталі

### Алгоритм навчання:
1. Завантаження та парсинг USD/UAH історії
2. Створення 11 ознак волатильності
3. Обчислення порогу на train (75-й перцентиль)
4. Time-aware split (80% train, 20% test)
5. Z-score стандартизація
6. Балансування класів через `sample_weight`
7. Навчання `InHouseGNB` з boosting
8. Оптимізація порогу класифікації (max F1)

### Обмеження API:
⚠️ **Без історичних даних в `/predict` точність критично падає!**
- Неможливо обчислити `rolling_std`, `ATR` без історії
- Використовується спрощений підхід (тільки для демо)
- Для продакшну потрібен batch prediction з повною історією

---

## 🎯 Рекомендації

### Для прогнозування волатильності валюти:
1. ✅ Використовуйте **LDA API** (`03-gaussian-classification-model`)
2. ✅ Або **Random Forest** / **XGBoost** для кращої точності
3. ✅ Додайте більше даних (2-3 роки історії)
4. ✅ Feature selection (видаліть корельовані ознаки)

### Для sentiment analysis:
- ✅ Naive Bayes чудово підходить для текстових даних!
- Використовуйте `MultinomialNB` для bag-of-words

---

## 📚 Структура проєкту

```
02-naive-bayessian-classifier/
├── api/
│   ├── main.py           # FastAPI додаток (оновлено!)
│   ├── requirements.txt  # Залежності
│   ├── Dockerfile        # Docker образ
│   ├── docker-compose.yml
│   └── README.md         # Цей файл
├── main.ipynb            # Jupyter notebook з аналізом
└── datasets/
    └── USD_UAH Historical Data.csv
```

---

## 🐳 Docker

```bash
# Збілдити образ
docker-compose build

# Запустити
docker-compose up

# API доступне на http://localhost:8001
```

---

## ❓ FAQ

**Q: Чому AUC такий низький?**  
A: Naive Bayes припускає незалежність ознак. Всі ознаки волатильності корельовані → припущення порушене → модель не працює.

**Q: Як покращити результат?**  
A: Не можна значно покращити Naive Bayes для цієї задачі. Використовуйте LDA або Random Forest.

**Q: Для чого тоді цей API?**  
A: Для навчальних цілей — демонструє, що Naive Bayes НЕ підходить для корельованих фінансових індикаторів.

**Q: Де Naive Bayes працює?**  
A: Для sentiment analysis новин, класифікації текстів, категоріальних даних з незалежними ознаками.

---

## 📞 Контакти

Для кращих результатів використовуйте:
- **LDA API:** `03-gaussian-classification-model/api`
- Або інші ML методи (Random Forest, XGBoost, LSTM)

---

**Висновок:** Naive Bayes не працює для статистики цін! 📉


